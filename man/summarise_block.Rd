% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise-block.R
\name{summarise_block}
\alias{summarise_block}
\alias{summarise_block.block}
\title{Summarise a block for the LLM prompt context}
\usage{
summarise_block(x, board, id, ...)

\method{summarise_block}{block}(x, board, id, ...)
}
\arguments{
\item{x}{A \code{block}.}

\item{board}{The current board snapshot, for resolving
cross-references. May be \code{NULL} -- currently unused by the
default method, reserved for overrides that want to surface
relationships.}

\item{id}{The id under which the block lives on the board.}

\item{...}{For future use.}
}
\value{
A single-line character scalar (preferred). Multi-line
output is accepted but inflates the prompt budget.
}
\description{
Generic backing the per-block lines in the dynamic system
prompt's board summary. The default method
\code{summarise_block.block} returns a compact one-line summary
(id, class, current arg values, modifiable keys). Block
authors override their class when the default is too generic
or too verbose for prompt context.
}
\details{
Parallel to \code{\link[=describe_block]{describe_block()}} (which produces the full
multi-line description used by the \code{describe_block} tool).
Use \code{summarise_block} when token density matters; use
\code{describe_block} when the user has explicitly asked for
detail.
}
