% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise-result.R
\name{summarise_result}
\alias{summarise_result}
\alias{summarise_result.default}
\title{Summarise a block result for the LLM}
\usage{
summarise_result(x, ...)

\method{summarise_result}{default}(x, ...)
}
\arguments{
\item{x}{A block result, as returned by the block's \code{result}
reactive.}

\item{...}{Passed to methods.}
}
\value{
Character vector of lines.
}
\description{
Generic dispatch on the result's class. The default forwards to
\code{\link[btw:btw_this]{btw::btw_this()}}, which ships methods for data frames, tibbles,
matrices, and falls back to a truncated \code{print()} for everything
else. Define methods to override the summary for specific result
classes (e.g. when \code{btw_this()} lacks a method or its output is
too verbose for token budgets).
}
\details{
Methods should keep their output bounded -- roughly 1-2 KB of
text per call is a sensible ceiling. The LLM pays in tokens for
every line returned, and an unbounded \code{print()} of a large
object will both blow the budget and bury the relevant
structural information in noise. The default (\code{btw::btw_this()})
truncates aggressively; overrides should too.
}
\examples{
summarise_result(head(iris))

}
