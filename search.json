[{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"vision","dir":"Articles > Design","previous_headings":"","what":"Vision","title":"Roadmap","text":"blockr.assistant blockr.dock extension puts chat interface next blockr board. chat driven ellmer equipped set tools — R functions exposed LLM — allow model inspect manipulate board state. Tool invocations translated update(...) reactive payloads interactive user actions use, assistant , board’s perspective, just another caller public extension API. intended user experience conversational pipeline construction: “Load iris dataset, drop species column, plot sepal length vs. width.” …producing corresponding blocks, links stack board.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Roadmap","text":"scope: dock_extension providing chat panel. Provider-neutral chat construction consuming board’s new_llm_model_option(). LLM tools CRUD operations blocks, links stacks. Block-parameter editing via blockr.core’s external-control mechanism (external_ctrl = TRUE blocks). Streamed responses tool-call telemetry via shinychat. Explicitly scope (now): Building LLM provider abstractions — ellmer handles . Application-level persistence wiring. extension’s constructor state shape make system prompt chat history round-trip blockr.dock’s standard ser/des (see Phase 1), integration triggers cross-session save/restore (e.g. via blockr.session) initial roadmap. Mutation safeguards (undo, dry-run, snapshot--mutation) — initial implementation applies tool calls directly board. Snapshotting likely revisited blockr.session exposes right hooks. Multi-board / multi-session reasoning — assistant context scoped single board mounted .","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"architecture-sketch","dir":"Articles > Design","previous_headings":"","what":"Architecture sketch","title":"Roadmap","text":"Concretely: new_assistant_extension() returns dock_extension whose UI shinychat::chat_mod_ui() panel whose server constructs ellmer::Chat board’s llm_model option, registers tool layer closures (board, update, session), hands chat shinychat::chat_mod_server(). tool R closure captures live board update references. Inspection tools read board$board. Mutation tools emit update(list(blocks = …, links = …, stacks = …)) payloads, write externally-controlled block reactives. chat’s system prompt composed fixed assistant persona plus dynamically generated board summary (block IDs, types, links, stacks, available block constructors).","code":"┌──────────────────────────────────────────────────────────────┐         │                       dock_board                             │         │                                                              │         │   ┌────────────────────┐     ┌────────────────────────────┐  │         │   │   board state      │ ←── │    assistant_extension     │  │         │   │   (blocks, links,  │     │                            │  │         │   │    stacks)         │     │  ┌──────────────────────┐  │  │         │   │                    │     │  │   chat panel (UI)    │  │  │         │   │   update reactive  │ ←── │  │   shinychat module   │  │  │         │   └────────────────────┘     │  └──────────┬───────────┘  │  │         │           ↑                  │             │              │  │         │           │                  │  ┌──────────┴───────────┐  │  │         │           │                  │  │  ellmer::Chat with   │  │  │         │           │                  │  │  registered tools    │  │  │         │           │                  │  │  ┌──────────────┐    │  │  │         │           └──────────────────┼──┤  │ board tools  │    │  │  │         │                              │  │  │ (CRUD)       │    │  │  │         │                              │  │  └──────────────┘    │  │  │         │                              │  └──────────────────────┘  │  │         │                              └────────────────────────────┘  │         └──────────────────────────────────────────────────────────────┘"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"architectural-decisions","dir":"Articles > Design","previous_headings":"","what":"Architectural decisions","title":"Roadmap","text":"Two decisions shape every phase deserve stated front: Provider neutrality via new_llm_model_option(). never construct ellmer chat client directly. board exposes llm_model board option whose value chat-constructor function signature function(system_prompt = NULL, params = NULL) returning ellmer::Chat. extension reads option calls . Providers, model names keys configured globally via blockr_option(\"chat_function\", …) — scope package. option always resolves usable function (falling back blockr.core’s default_chat), extension never needs handle missing chat backend. Narrow tools, auto-batched dispatch. LLM sees many small, focused tools (add_block, add_link, …) rather one monolithic modify_board. tool call dispatch update(...) immediately; stages change pending payload held inside extension server. Validation runs current board merged pending payload, per-tool feedback meaningful. assistant’s turn completes, accumulated payload flushed single update(...) call. combination gives us: LLM-friendly tool ergonomics (small, focused calls, easy describe). single atomic dispatch per user turn (one validation pass, one re-render, one logical snapshot point future blockr.session integration). “add link block” ordering trap disappears within turn — ordering stage operations irrelevant long final payload valid. staging layer central architectural deliverable package; every mutation tool thin façade .","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"decomposition-into-phases","dir":"Articles > Design","previous_headings":"","what":"Decomposition into phases","title":"Roadmap","text":"phase corresponds design doc vignettes/design/, named <n>-<topic>.Rmd. files deliberately excluded package tarball via .Rbuildignore — installed vignettes (R CMD check rejects digit-prefixed vignette filenames) pkgdown walks vignettes/ recursively picks articles served articles/design/<n>-<topic>.html. Phases ordered package compiles demos something end one.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"phase-1-extension-shell-with-ellmer-chat","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Phase 1 — Extension shell with ellmer chat","title":"Roadmap","text":"File: 1-shell.Rmd new_assistant_extension() constructor, dock UI/server, shinychat::chat_mod_ui / chat_mod_server panel, ellmer::Chat constructed board’s llm_model option. Includes day one: Streaming responses, history, basic token telemetry. visible “stop” affordance — provided free shinychat::chat_mod_server(), constructs ellmer::stream_controller() internally wires cancel input. work us beyond verifying end--end. default, overridable assistant persona shipped package — configurable via system_prompt argument new_assistant_extension(). default persona short scoped “blockr board assistant”; subsequent phases add dynamic board context top. state shape round-trips constructor arguments (system_prompt, messages) via blockr.dock’s ser/des. Constructor signature ↔︎ state keys structural contract (see 1-shell.Rmd implications). Phase 1 also adds messages = NULL constructor argument saved conversation can seeded back chat restore. Model parameters (temperature, max tokens, …) deliberately extension’s surface — belong chat constructor, supplied via blockr_option(\"chat_function\", …) board’s llm_model option. tools yet — assistant can hold conversation board-blind.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"phase-2-read-only-tool-layer","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Phase 2 — Read-only tool layer","title":"Roadmap","text":"File: 2-read-tools.Rmd Establish “tool kit” abstraction: closures (board, update, session), JSON-schema argument definitions via ellmer::type_*, result shaping (strings, structured lists, errors). Implement read-inspection tools: list_blocks, describe_block list_links, list_stacks list_available_block_types get_block_result_preview phase locks patterns later mutation phases follow.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"phase-3-staging-and-dispatch-model","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Phase 3 — Staging and dispatch model","title":"Roadmap","text":"File: 3-staging.Rmd architectural keystone. new LLM-facing tools, clean foundation Phase 4. pending-update reactiveVal private extension server. “merge current board + pending payload” helper tools validate . turn-completion hook flushes pending payload via update(...) resets . Error formatting tool results (model can self-correct). Behaviour flush failure (partial rollback, error user, error model).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"phase-4-mutation-tools","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Phase 4 — Mutation tools","title":"Roadmap","text":"File: 4-mutation-tools.Rmd thin façades Phase 3 staging layer: Blocks: add_block(type, args), remove_block(id), modify_block(id, args) (via external_ctrl reactives). Links: add_link(, , input), remove_link(id). Stacks: add_stack(blocks, name), remove_stack(id), modify_stack(id, blocks, name). Design decisions nail : ID generation policy, validation constructor args staging, behaviour external control available modify_block target, link validity checks (referenced blocks exist, target input exists, cycles).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"phase-5-context-examples-and-polish","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Phase 5 — Context, examples and polish","title":"Roadmap","text":"File: 5-polish.Rmd Dynamic board-summary system prompt (refreshed turn). Model-facing error formatting recovery hints. Example apps inst/examples/. top-level tutorial vignette intro.Rmd. README, pkgdown site setup. new features beyond polish context layer.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"splitting-phase-4-if-it-grows","dir":"Articles > Design","previous_headings":"Decomposition into phases","what":"Splitting Phase 4 if it grows","title":"Roadmap","text":"Phase 4 turns larger expected, natural split block mutation vs. link + stack mutation. Blocks carry bulk complexity (external control, registry lookup, ID generation); links stacks mechanical block patterns place. cheap retroactively — design doc cleaves boundary.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/0-roadmap.html","id":"future-work-out-of-the-initial-roadmap","dir":"Articles > Design","previous_headings":"","what":"Future work (out of the initial roadmap)","title":"Roadmap","text":"flagged don’t lose , explicitly phases initial roadmap: Snapshot / undo — integrate blockr.session snapshot board state mutation expose undo tool LLM. Requires hooks blockr.session don’t yet exist. Conversation persistence wiring — Phase 1 already ships state shape constructor surface needed chat history round-trip via ser/des. ’s missing integration actually triggers save/restore — either blockr.session via shinychat::chat_mod_server(bookmark_on_*). Per-block-type tool registration — allow block packages contribute tools specific blocks (e.g. blockr.dplyr block exposing set_filter_expression tool). Multi-agent / multi-tool-call orchestration — explicit support long-running plans, tool-call retries, structured planning. UI affordances tool calls — inline “approve change” UI chat, surfacing tool diffs user apply.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"goal","dir":"Articles > Design","previous_headings":"","what":"Goal","title":"Extension shell with `ellmer` chat","text":"Phase 1 delivers mountable dock_extension whose UI shinychat panel whose server constructs ellmer::Chat board’s llm_model option. end Phase 1 package builds, extension can added dock_board, user can hold streaming, cancellable conversation model. assistant awareness board yet — Phase 2.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Extension shell with `ellmer` chat","text":": new_assistant_extension() constructor returning dock_extension. chat panel UI based shinychat::chat_mod_ui(). extension server reads llm_model board options, invokes constructor, hands resulting ellmer::Chat shinychat::chat_mod_server(). short, overridable default system prompt. state shape round-trips system prompt full conversation blockr.dock’s ser/des. demo app inst/examples/ mounts extension dock_board one two starter blocks. (deferred later phases): awareness board state, blocks, links, stacks. Tool registration (Phase 2 4). Dynamic board summary system prompt (Phase 5). Application-level persistence wiring. extension built history can round-trip ser/des, cross-session save/restore (e.g. via blockr.session) phase.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"architectural-decisions","dir":"Articles > Design","previous_headings":"","what":"Architectural decisions","title":"Extension shell with `ellmer` chat","text":"State shape mirrors constructor signature. blockr.dock’s extension ser/des serializes extension server’s state list entry--entry (reval_if) restore calls constructor entries spliced named arguments (.call(ctor_fun, c(payload, …)), see blockr.dock/R/utils-serdes.R). structural consequence: every key state must valid argument new_assistant_extension(), conversely every constructor argument affects runtime behaviour appear state want save/restore fidelity. decision shapes rest doc. Provider neutrality via new_llm_model_option(). never construct ellmer chat client directly. board exposes llm_model board option whose value chat-constructor signature function(system_prompt = NULL, params = NULL) returning ellmer::Chat. blockr.core guarantees usable fallback (default_chat) extension never needs handle missing backend. Chat client constructed per session. Re-constructing every llm_model option change force chat_mod_server re-mount lose history. Phase 1 option read extension-server start; changing models mid-session requires session restart. Lifting restriction future work. Stream cancellation delegated shinychat, built . shinychat::chat_mod_server() already instantiates ellmer::stream_controller() internally wires input$chat_cancel controller$cancel(). stop button surfaces chat_mod_ui() chrome. Phase 0 roadmap entry “visible stop affordance” therefore costs nothing phase — verify works end--end demo.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"public-api","dir":"Articles > Design","previous_headings":"","what":"Public API","title":"Extension shell with `ellmer` chat","text":"system_prompt — character scalar NULL. NULL, package default used. user-supplied string replaces default verbatim; concatenate. Callers want extend default can read via unexported default_system_prompt(). messages — optional list recorded turns seed conversation . -disk shape output ellmer::contents_record() per turn (see Wire format ). NULL means start empty conversation. ... — forwarded new_dock_extension() (notably options = pkg = provenance argument). Notably absent: params argument. Model parameters (temperature, max tokens, …) belong chat constructor, extension. caller wants different params register chat constructor bakes — either global blockr_option(\"chat_function\", …) named alternative selected via board’s llm_model option. second params knob extension create two ways thing, precedence question set, force us design JSON ser/des ellmer::params() (S7 object) just round-trip state. Class: assistant_extension, inheriting dock_extension.","code":"new_assistant_extension(   system_prompt = NULL,   messages      = NULL,   ... )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"extension-ui","dir":"Articles > Design","previous_headings":"","what":"Extension UI","title":"Extension shell with `ellmer` chat","text":"chat_mod_ui() renders message list, input box stop button. namespace inner id (\"chat\") later phases can add sibling outputs (token counter, debug panel) without colliding chat_mod_ui’s internal ids.","code":"asst_ext_ui <- function(id, board, ...) {   shinychat::chat_mod_ui(NS(id, \"chat\")) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"extension-server","dir":"Articles > Design","previous_headings":"","what":"Extension server","title":"Extension shell with `ellmer` chat","text":"notes structure: system_prompt messages argument captured new_assistant_extension() enclosing scope. system_prompt stays plain string throughout session. messages argument consumed seed client$set_turns() local reactiveVal name; reactive form rest server reads writes. get_board_option_value(\"llm_model\", session) blockr.core’s accessor board options inside Shiny module. returns chat constructor chat_name attribute can surface later telemetry. accessor reads reactive value, wrap isolate() – chat client constructed module startup must re-fire option later changes. messages tracks recorded form conversation. append turn boundary (mod$last_input user’s submit, mod$last_turn model’s response completion) rather re-recording whole list — contents_record growing list turns O(n²) conversation. length(turns) < length(recorded) branch handles edge case something external (e.g. mod$clear()) shrinks client’s turns; re-sync rather diverge. return shape list(state = …) contract documented dock_extension servers (blockr.dock/R/ext-class.R). Every key state matches constructor argument name — makes ser/des coherent. live ellmer::Chat instance chat_mod_server return (mod) deliberately state. runtime concerns (tool registration Phase 2, turn-completion observers Phase 3), serializable, constructor arguments. Exposing externally — later phases need — separate decision ser/des.","code":"asst_ext_srv <- function(id, board, update, ...) {    moduleServer(     id,     function(input, output, session) {        chat_ctor <- isolate(get_board_option_value(\"llm_model\", session))       sys_prompt <- system_prompt %||% default_system_prompt()        client <- chat_ctor(system_prompt = sys_prompt)        if (length(messages)) {         client$set_turns(lapply(messages, ellmer::contents_replay))       }        mod <- shinychat::chat_mod_server(\"chat\", client)        messages <- reactiveVal(messages %||% list())        record_new_turns <- function() {         recorded <- messages()         turns <- client$get_turns()          if (length(turns) > length(recorded)) {           new_idx <- seq.int(length(recorded) + 1L, length(turns))           messages(             c(recorded, lapply(turns[new_idx], ellmer::contents_record))           )         } else if (length(turns) < length(recorded)) {           messages(lapply(turns, ellmer::contents_record))         }       }        observeEvent(mod$last_input(), record_new_turns(), ignoreNULL = TRUE)       observeEvent(mod$last_turn(),  record_new_turns(), ignoreNULL = TRUE)        list(         state = list(           system_prompt = sys_prompt,           messages      = messages         )       )     }   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"wire-format-for-messages","dir":"Articles > Design","previous_headings":"","what":"Wire format for messages","title":"Extension shell with `ellmer` chat","text":"Corrected. “opaque blobs” shape written state verbatim, survive board’s JSON round trip. jsonlite reads \"version\": 1 back integer, type-strict check ellmer::contents_replay() rejects, typed props come back mistyped — tokens list, \"NA\" strings seen failing S7 validation. replay runs board-server observer, board saved chat turn aborted whole board restore. record/replay pair still turns Turn data, result now stored single opaque jsonlite::serializeJSON() string, preserves R’s types exactly round-trips board file unchanged. saved window bounded blockr.chat_save_turns option, read save time, raw provider response json stripped, truncation trimmed whole exchanges window never opens tool result whose request fell outside . structure therefore still payload — simply longer thing written disk. use ellmer::contents_record() / ellmer::contents_replay() round-trip pair. Output plain JSON — opaque blobs — round-trip lossless across turn shapes (text, tool calls, multi-content). simplest exchange: Verbosity cost lossless round-tripping — assistant turns carry tool calls (Phase 4) token telemetry faithfully preserved. deliberately roll {role, content} shape: lossy Phase 4 duplicate work ellmer already maintains.","code":"[   { \"version\": 1, \"class\": \"ellmer::UserTurn\",     \"props\": {       \"contents\": [         { \"version\": 1, \"class\": \"ellmer::ContentText\",           \"props\": { \"text\": \"hi\" } }       ]     }   },   { \"version\": 1, \"class\": \"ellmer::AssistantTurn\",     \"props\": {       \"contents\": [         { \"version\": 1, \"class\": \"ellmer::ContentText\",           \"props\": { \"text\": \"hello back\" } }       ],       \"json\": [], \"tokens\": [\"NA\",\"NA\",\"NA\"],       \"cost\": \"NA\", \"duration\": \"NA\"     }   } ]"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"default-system-prompt","dir":"Articles > Design","previous_headings":"","what":"Default system prompt","title":"Extension shell with `ellmer` chat","text":"“, yet” line deliberate: Phase 1 model board-blind, without explicit instruction happily hallucinate CRUD vocabulary . Replacing prompt becomes important Phase 2 real tools exist; revisit text . prompt lives string constant R/system-prompt.R. single constant fine now; file-based prompts can come later start composing multiple fragments (persona + board summary + tool catalogue).","code":"You are a helpful assistant embedded next to a blockr data analysis board. In future versions you will be able to inspect and manipulate the board; for now you can only talk to the user. Answer concisely. Do not invent tool calls or claim to have changed the board — you cannot, yet."},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"telemetry","dir":"Articles > Design","previous_headings":"","what":"Telemetry","title":"Extension shell with `ellmer` chat","text":"ellmer::Chat exposes per-turn token accounting via client$tokens() client$last_turn()$tokens. Phase 1 surfaces small verbatimTextOutput(\"tokens\") beneath chat: output updated observer mod$last_turn. Cost estimation scope Phase 1 (requires per-model price table yet maintain); raw token counts shown. Token counts added state. recomputable recorded messages (per-turn tokens field preserved contents_record), exposing separately redundant commit us key isn’t constructor argument.","code":"input: 312   output: 84   total this turn: 396"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"demo-app","dir":"Articles > Design","previous_headings":"","what":"Demo app","title":"Extension shell with `ellmer` chat","text":"inst/examples/populated-board/app.R mounts extension small board: Acceptance criteria Phase 1: App launches without warnings board llm_model unset (.e. default_chat fallback exercised). streaming response renders chunk--chunk chat panel. Clicking stop button mid-stream stops stream leaves partial-turn entry history (chat_mod_server default behaviour). token line updates turn. Serialize board three-turn conversation, restore fresh session, confirm: () conversation visible chat panel, (b) next user message correctly extends restored history, (c) system prompt survived round-trip. devtools::check() passes 0/0/0.","code":"library(blockr.core) library(blockr.dock) library(blockr.assistant)  board <- new_dock_board(   blocks     = list(new_dataset_block(\"iris\")),   extensions = list(new_assistant_extension()) )  serve(board)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"files-added-in-this-phase","dir":"Articles > Design","previous_headings":"","what":"Files added in this phase","title":"Extension shell with `ellmer` chat","text":"R/extension.R — new_assistant_extension(), asst_ext_ui(), asst_ext_srv(). R/system-prompt.R — default_system_prompt(). R/utils.R — %||% small helpers shared later phases. inst/examples/populated-board/app.R — demo app. tests/testthat/test-extension.R — smoke tests (is_dock_extension(), server runs shiny::testServer() default_chat mock, ser/des round-trip).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/1-shell.html","id":"known-limitations-carried-into-later-phases","dir":"Articles > Design","previous_headings":"","what":"Known limitations carried into later phases","title":"Extension shell with `ellmer` chat","text":"Changing llm_model option mid-session effect — client captured server start. Either documented known limitation addressed explicit “reset chat” button later. board awareness: chat answer “blocks ?” questions yet. entire subject Phase 2. application-level persistence trigger. Phase 1 makes history serializable; ’s missing integration actually saves restores boards across sessions.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"goal","dir":"Articles > Design","previous_headings":"","what":"Goal","title":"Read-only tool layer","text":"Phase 2 turns board-blind chat shell Phase 1 assistant can look board. register set read-ellmer tools chat client, implemented closure extension server’s (board, update, session) references. end Phase 2 user can ask “blocks ?”, “my_subset ?”, “result my_filter look like?” get answer grounded live board state. mutation tools yet — Phase 4, ride top staging layer added Phase 3. phase introduces three S3 generics rest package — downstream packages — lean : summarise_result(x, ...) turns block’s evaluated output terse, LLM-shaped text. default forwards btw::btw_this(); override per-class default isn’t enough. describe_block(x, board, id, ...) returns multi-line description describe_block tool reports back model. default method operates base block class via registry’s generic metadata; block packages can override specific class generic output isn’t enough. describe_stack(x, ...) stack counterpart: one-line (short multi-line) description per stack, returned alongside basic enumeration fields list_stacks. default method base stack covers id / name / blocks; stack-class extensions (e.g. blockr.dag’s dag_stack, adds colour attribute) override class surface extra fields. fourth axis — letting sibling dock extensions contribute tools — considered deferred. depends upstream changes blockr.dock (init ordering, sibling state access, ultimately external-control channel) scope Phase 2. See #4 blockr.dock#129.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Read-only tool layer","text":": Six tool factories signature function(board, update, session) -> ellmer::ToolDef, living R/tools-read.R. single register_read_tools() helper calls factory registers ToolDef chat client. list_blocks describe_block list_links list_stacks list_available_blocks get_block_result revised default system prompt drops “, yet” hedge instead introduces inspection toolkit. demo app inst/examples/populated-board/ mounting extension small concrete board: dataset_block(\"iris\") feeding subset_block. Two blocks, one link, evaluated results — enough exercise list_blocks, describe_block, list_links, get_block_result end--end. Unit tests exercising tool testServer-driven board fixture fakes non-reactive parts. (deferred later phases): mutation. add_* / remove_* / modify_* tools. staging layer (Phase 3). Read-tools pending state merge — read board structure live results directly. Dynamic board summary system prompt (Phase 5). persona rewritten mention tools, prompt still static — model discovers board tool calls, prompt injection.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"tools-as-closures-over-board-update-session","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Tools as closures over (board, update, session)","title":"Read-only tool layer","text":"tool built factory signature function(board, update, session) -> ellmer::ToolDef. factory closes extension references constructs ToolDef server start. implementation function inside ToolDef closure references, always sees current board via board$board / board$blocks.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"extensibility-via-s3-generics","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Extensibility via S3 generics","title":"Read-only tool layer","text":"Two axes need extensibility surfaces — S3 generics, following patterns blockr ecosystem already uses things like block_metadata() block_inputs(). Per-block-class — describe_block() generic. describe_block tool registered ellmer takes id, fetches corresponding block board$blocks, delegates describe_block(block, board, id). Default behaviour lives describe_block.block produces description registry’s generic block metadata (constructor name, argument descriptions, external-control flag, current parameter values, incoming links). block author whose block class deserves richer description simply defines describe_block.<their_block_class>. coupling blockr.assistant beyond @export method — don’t even import generic @method describe_block <class> + roxygen handles registration. default steady state ~blocks; overrides escape hatch, requirement. Per-stack-class — describe_stack() generic. shape, applied stacks. Unlike blocks, separate describe_stack tool — list_stacks tool calls describe_stack(stack) every stack lists surfaces result alongside basic fields. default describe_stack.stack returns short summary covering id / name / blocks. Stack classes carry extra attributes — blockr.dag::dag_stack adds colour, future variants may add layout hints — override describe_stack.<their_stack_class> surface . blockr.assistant focuses default base stack class ; everything class-specific downstream’s call. Sibling dock extensions — deliberately exposed Phase 2. explored third generic — assistant_tools(extension, ...) — let sibling dock extensions contribute tools. interesting use case isn’t reading sibling state manipulating (e.g. “set markdown content md extension”), needs external-control mechanism extensions blockr.dock doesn’t yet provide. Even read-variant requires upstream changes: extension servers run via lapply() ordering guarantee, API read sibling-server return values. Shipping generic without upstream pieces constrain stateless contributions, materially less interesting per-block axis. design preserved #4; upstream prerequisite tracked blockr.dock#129.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"result-summarisation-via-summarise_result","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Result summarisation via summarise_result()","title":"Read-only tool layer","text":"block’s evaluated result R object — data.frame, ggplot, list, fitted model, raster, large list large lists. Two consequences: JSON-serialising full object non-starter. blows token budget conveys nothing model can act . need LLM-shaped summary whose format depends object’s class. introduce summarise_result() exported S3 generic. default method forwards btw::btw_this(), already ships class-specific methods data frames (skimr-style), tibbles, matrices, etc., falls back truncated print() everything else. generic gives us single hook cases btw_this() either missing method returns something ’d rather replace: get_block_result consumes generic: board$blocks reachable extension callback blockr.core’s board server stores reactiveValues later passes make_read_only() (blockr.core/R/board-server.R:60-70, R/utils-shiny.R:13). make_read_only wrapper preserves reactiveValues semantics, board$blocks[[id]]$server$result() live reactive — calling inside tool function (runs chat module’s reactive context chat_mod_server’s turn handling) returns current evaluated result. Phase 2 ships default summarise_result() method. Bespoke methods come hit result class default output unsatisfactory — point override one method definition, plumbing.","code":"#' Summarise a block result for the LLM #' #' Generic dispatch on the result's class. The default forwards to #' [btw::btw_this()]. Define methods to override the summary for #' specific result classes (e.g. when btw lacks a method or its #' output is too verbose for token budgets). #' #' Methods should keep their output bounded — roughly 1–2 KB of #' text per call is a sensible ceiling. The LLM pays in tokens for #' every line returned, and an unbounded `print()` of a large #' object will both blow the budget and bury the relevant #' structural information in noise. The default (`btw::btw_this()`) #' truncates aggressively; overrides should too. #' #' @param x A block result, as returned by the block's `result` #'   reactive. #' @param ... Passed to methods. #' @return Character vector of lines. #' @export summarise_result <- function(x, ...) {   UseMethod(\"summarise_result\") }  #' @export summarise_result.default <- function(x, ...) {   btw::btw_this(x, ...) } tool_get_block_result <- function(board, update, session) {    ellmer::tool(     function(id) with_tool_errors(\"get_block_result\", {        blks <- isolate(board$blocks)       if (!id %in% names(blks)) {         return(sprintf(           \"No block with id %s. Call list_blocks first.\", id         ))       }        res <- tryCatch(         isolate(blks[[id]]$server$result()),         error = function(e) e       )        if (inherits(res, \"error\")) {         return(sprintf(           \"Block %s has not evaluated successfully: %s\",           id, conditionMessage(res)         ))       }        paste(summarise_result(res), collapse = \"\\n\")     }),     name = \"get_block_result\",     description = paste(       \"Return a short text summary of a block's current evaluated\",       \"output. Data frames are summarised with skimr-style stats;\",       \"other objects fall back to a truncated print. Returns an\",       \"error string if the block has not evaluated successfully.\"     ),     arguments = list(       id = ellmer::type_string(         \"Block id, as returned by list_blocks.\"       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"read-only-tools-snapshot-they-do-not-subscribe","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Read-only tools snapshot, they do not subscribe","title":"Read-only tool layer","text":"Every tool function returns value derived current read board (board$board, board$blocks, block registry). set observe/reactive machinery — nothing push, pull. keeps tool layer cheap free leaked observers chat client rebuilt. reactive reads inside tool bodies wrapped isolate(). shinychat::chat_mod_server() invokes tool functions inside observer chain, un-isolated read board$board register enclosing observer dependent board slot. subsequent board mutation re-fire observer lifetime reactive context, want — tools want one-shot snapshot. Wrapping every reactive read (board$board, board$blocks, ...$server$result()) isolate() gives us current value without dependency. isolate() also safe called non-reactive contexts (e.g. unit tests), can pay wrapping cost uniformly. consequence: user adds new block mid-conversation asks “blocks ?”, next list_blocks call sees new block correctly, tool reads current reactive board state call time (just without subscribing ).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"result-shape-structured-but-stringy","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Result shape: structured-but-stringy","title":"Read-only tool layer","text":"ellmer serialises tool return values JSON model. return data.frames structure helps model (list_blocks, list_links, list_stacks, list_available_blocks) plain strings prose clearer (describe_block, get_block_result). wrap tool returns custom envelope: ellmer’s auto-conversion handles JSON encoding, “looks like error message” string cheapest possible recovery signal model something goes wrong.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"argument-schemas-via-ellmertype_","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Argument schemas via ellmer::type_*","title":"Read-only tool layer","text":"Tools take arguments use ellmer::type_string() schema. invent argument-validation layer — ellmer hands schema model routes call. bad argument arrives, tool’s checks (e.g. “id board_block_ids()?”) surface error, returned structured error string (see Error formatting ).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"tool-catalogue","dir":"Articles > Design","previous_headings":"","what":"Tool catalogue","title":"Read-only tool layer","text":"entry lists tool name, description shown model, argument schema, return shape.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"list_blocks","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"list_blocks","title":"Read-only tool layer","text":"Description. “List blocks board. Returns data frame one row per block: id, type, name, package.” Arguments. none. Returns. data.frame columns id, type, name, package. type class(block)[[1L]]; name package come block_metadata(block).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"describe_block","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"describe_block","title":"Read-only tool layer","text":"Scope. Board blocks — .e. blocks currently mounted board. registry block types (block constructors user add hasn’t), available metadata already surfaced row-per-type list_available_blocks, separate registry-side describe tool redundant. Description. “Describe block currently board: constructor name, current parameter values, whether externally controllable, blocks feed .” Arguments. id = ellmer::type_string(\"Block id, returned list_blocks.\"). Returns. character scalar multi-line text (model sees newline-joined string). tool’s implementation looks block board$blocks, delegates exported describe_block(block, board, id) S3 generic — returns character vector lines — collapses paste(..., collapse = \"\\n\") returning ellmer. default method describe_block.block produces description registry’s generic metadata plus live instance state (current arg values, external-control flag, incoming links). Block authors override describe_block.<their_block_class> generic output insufficient. Pre-formatted prose works better model nested list, description mixes structured fields free-form values.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"list_links","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"list_links","title":"Read-only tool layer","text":"Description. “List links blocks: row gives link id, source block, destination block, input destination fed.” Arguments. none. Returns. .data.frame(board_links(board$board)) — columns id, , , input.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"list_stacks","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"list_stacks","title":"Read-only tool layer","text":"Description. “List stacks board. stack groups blocks name; returns one row per stack id, name, comma-separated block ids, class-specific description.” Arguments. none. Returns. data.frame columns id, name, blocks, description. first three come board_stacks(board) / stack_blocks(s). description paste(describe_stack(stack), collapse = \"\\n\") stack, dispatched stack’s class; default describe_stack.stack returns short summary, stack-class extensions (e.g. blockr.dag::dag_stack) override class add fields (colour, layout, …). Methods return character vector lines; tool collapses single string per row.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"list_available_blocks","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"list_available_blocks","title":"Read-only tool layer","text":"Description. “List every registered block constructor — block types user can add board. One row per type id, name, package, category, description, model-facing construction guidance, constructor’s argument descriptions types, worked examples.” Arguments. none. Returns. data.frame columns id, name, package, category, description, guidance (model-facing construction notes, NA none), arguments (list-column mapping argument name description , declared, JSON-Schema type descriptor), examples (list-column complete worked configurations keyed argument name, empty none), inputs. inputs read blockr.core’s block_metadata(); inputs derived constructor’s instantiated block_inputs(). id column registry uid add_block accepts. Rationale skipping describe_available_block tool. Everything block_metadata() knows registered type static bounded — name, description, category, package, argument descriptions types, construction guidance, worked examples — fits one row. Splitting enumeration tool detail tool way list_blocks / describe_block split buy us nothing: “runtime state” unmounted block type, per-row payload comparable size describe_block response. One tool, one round-trip. one tool whose return value fully determined package-load-time state block registry, dependency board. still build factory shape uniformity.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"get_block_result","dir":"Articles > Design","previous_headings":"Tool catalogue","what":"get_block_result","title":"Read-only tool layer","text":"Description. “Return short text summary block’s current evaluated output. Data frames summarised skimr-style stats; objects fall back truncated print. Returns error string block evaluated successfully.” Arguments. id = ellmer::type_string(\"Block id, returned list_blocks.\"). Returns. Character scalar — concatenated output summarise_result(result). See Result summarisation via summarise_result() generic default.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"system-prompt-revision","dir":"Articles > Design","previous_headings":"","what":"System prompt revision","title":"Read-only tool layer","text":"Phase 1 prompt ends “invent tool calls claim changed board — , yet.” Phase 2 replaces prompt one names new toolkit explicitly: list tools hard-coded prompt. deliberately introduce dynamic prompt templating — Phase 5 owns . tool added removed phases, updating string one find--replace.","code":"You are an assistant embedded next to a blockr data analysis board. You have a set of inspection tools that let you read the board's current state: list_blocks, describe_block, list_links, list_stacks, list_available_blocks, and get_block_result. Prefer calling a tool over guessing when the user asks about the board. You cannot modify the board yet; future versions will add tools for that. When you call a tool, wait for its result before continuing your reply."},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"describe_block-generic-rdescribe-block-r","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"describe_block() generic — R/describe-block.R","title":"Read-only tool layer","text":"","code":"#' Describe a block for the LLM #' #' Generic backing the `describe_block` assistant tool. The default #' method `describe_block.block` reports the block's constructor #' name, current parameter values, external-control flag, and #' incoming links from the registry's generic block metadata. Block #' authors override their class when the default isn't enough. #' #' @param x A `block`. #' @param board The current board snapshot, for resolving link #'   metadata. #' @param id The id under which the block lives on the board (the #'   name attribute of the enclosing `blocks` object — block objects #'   themselves do not carry an id because ids must be unique while #'   block-level fields are user-supplied and need not be). #' @param ... For future use. #' @return Character vector of lines (consistent with #'   [summarise_result()] and [describe_stack()]). The tool that #'   consumes this collapses with `paste(..., collapse = \"\\n\")` #'   before returning to ellmer. #' @export describe_block <- function(x, board, id, ...) UseMethod(\"describe_block\")  #' @rdname describe_block #' @export describe_block.block <- function(x, board, id, ...) {    meta <- block_metadata(x)   args <- initial_block_state(x)   ctrl <- block_external_ctrl_vars(x)   inc  <- board_links(board)[board_links(board)$to == id, ]    c(     sprintf(\"Block %s (%s, from %s)\", id, meta$name, meta$package),     sprintf(\"  %s\", meta$description),     \"\",     \"Arguments:\",     if (length(args)) {       chr_ply(         names(args),         function(nm) sprintf(\"  %s: %s\", nm, format(args[[nm]]))       )     } else {       \"  (none)\"     },     \"\",     sprintf(       \"External control: %s\",       if (!length(ctrl)) \"no\" else paste(ctrl, collapse = \", \")     ),     \"\",     \"Incoming links:\",     if (nrow(inc)) {       chr_ply(         seq_len(nrow(inc)),         function(i) sprintf(\"  %s <- %s (input: %s)\",                             inc$id[i], inc$from[i], inc$input[i])       )     } else {       \"  (none)\"     }   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"describe_stack-generic-rdescribe-stack-r","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"describe_stack() generic — R/describe-stack.R","title":"Read-only tool layer","text":"","code":"#' Describe a stack for the LLM #' #' Generic backing the `description` column of the `list_stacks` #' tool. The default method `describe_stack.stack` summarises the #' base stack fields (id, name, comma-separated blocks). Stack #' classes that carry extra attributes (e.g. `blockr.dag::dag_stack` #' with a colour) override their class to surface those attributes. #' #' @param x A `stack`. #' @param ... For future use. #' @return Character vector of lines (consistent with #'   [summarise_result()] and [describe_block()]). The tool that #'   consumes this collapses with `paste(..., collapse = \"\\n\")` #'   before returning to ellmer. #' @export describe_stack <- function(x, ...) UseMethod(\"describe_stack\")  #' @rdname describe_stack #' @export describe_stack.stack <- function(x, ...) {   sprintf(     \"stack '%s' (blocks: %s)\",     stack_name(x) %||% \"<unnamed>\",     paste(stack_blocks(x), collapse = \", \")   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"tool-factories-rtools-read-r","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"Tool factories — R/tools-read.R","title":"Read-only tool layer","text":"","code":"register_read_tools <- function(client, board, update, session) {    client$register_tool(tool_list_blocks(board, update, session))   client$register_tool(tool_describe_block(board, update, session))   client$register_tool(tool_list_links(board, update, session))   client$register_tool(tool_list_stacks(board, update, session))   client$register_tool(     tool_list_available_blocks(board, update, session)   )   client$register_tool(     tool_get_block_result(board, update, session)   )    invisible(client) }  with_tool_errors <- function(name, expr) {    tryCatch(     expr,     error = function(e) {       sprintf(\"%s failed: %s\", name, conditionMessage(e))     }   ) }  tool_list_blocks <- function(board, update, session) {    ellmer::tool(     function() with_tool_errors(\"list_blocks\", {        b <- isolate(board$board)       ids <- board_block_ids(b)        if (!length(ids)) {         return(           data.frame(             id      = character(),             type    = character(),             name    = character(),             package = character()           )         )       }        blks <- board_blocks(b)       meta <- lapply(blks, block_metadata)        data.frame(         id      = ids,         type    = chr_ply(blks, function(x) class(x)[[1L]]),         name    = chr_xtr(meta, \"name\"),         package = chr_xtr(meta, \"package\"),         row.names = NULL       )     }),     name        = \"list_blocks\",     description = \"List all blocks on the board with id, type, name, package.\",     arguments   = list()   ) }  tool_describe_block <- function(board, update, session) {    ellmer::tool(     function(id) with_tool_errors(\"describe_block\", {        brd <- isolate(board$board)       blks <- board_blocks(brd)       if (!id %in% names(blks)) {         return(sprintf(           \"No block with id %s. Call list_blocks first.\", id         ))       }        paste(         describe_block(blks[[id]], board = brd, id = id),         collapse = \"\\n\"       )     }),     name        = \"describe_block\",     description = \"Describe a block in detail: constructor, args, external_ctrl, incoming links.\",     arguments   = list(       id = ellmer::type_string(\"Block id, as returned by list_blocks.\")     )   ) }  tool_list_stacks <- function(board, update, session) {    ellmer::tool(     function() with_tool_errors(\"list_stacks\", {        stks <- board_stacks(isolate(board$board))       if (!length(stks)) {         return(           data.frame(             id          = character(),             name        = character(),             blocks      = character(),             description = character()           )         )       }        data.frame(         id          = names(stks),         name        = chr_ply(stks, function(s) stack_name(s) %||% NA_character_),         blocks      = chr_ply(stks, function(s) paste(stack_blocks(s), collapse = \", \")),         description = chr_ply(stks, function(s) {           paste(describe_stack(s), collapse = \"\\n\")         }),         row.names   = NULL       )     }),     name        = \"list_stacks\",     description = \"List all stacks with id, name, blocks, and a class-specific description.\",     arguments   = list()   ) }  # list_links, list_available_blocks, and # get_block_result follow the same skeleton."},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"extension-server-diff","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"Extension server diff","title":"Read-only tool layer","text":"Tool registration happens client built set_turns(), restored conversation referenced tools resolves correctly replayed.","code":"asst_ext_srv <- function(system_prompt, messages) {    function(id, board, update, ...) {      moduleServer(       id,       function(input, output, session) {          chat_ctor <- get_board_option_value(\"llm_model\", session)         sys_prompt <- coal(system_prompt, default_system_prompt())          client <- chat_ctor(system_prompt = sys_prompt)          register_read_tools(client, board, update, session)         # new          if (length(messages)) {           client$set_turns(lapply(messages, ellmer::contents_replay))         }          # ... unchanged from Phase 1 ...       }     )   } }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"error-formatting","dir":"Articles > Design","previous_headings":"","what":"Error formatting","title":"Read-only tool layer","text":"Tools return errors plain prose strings model can self-correct rather seeing opaque crash. Two layers: Argument-level errors (unknown block id, unknown stack id) return short factual sentence pointing model right discovery tool — e.g. “block id X. Call list_blocks first.” deliberate early returns, exceptions. Implementation-level errors (block_metadata() call throws, custom describe_block.<class> method errors , summarise_result override raises) caught internal helper with_tool_errors(name, expr) wraps every tool body tryCatch. handler translates error \"<tool_name> failed: <conditionMessage(e)>\". Tools may also catch specific error conditions inline condition needs distinctive message — e.g. get_block_result’s inner tryCatch around result() flags “block X evaluated successfully” rather just generic-tool-failure. inline catches live inside with_tool_errors, anything don’t explicitly handle still hits outer catch-. format plain text, JSON envelope: model already treats tool returns text, structured-error type designed Phase 4 mutations even arrive. Phase 4 revisit mutation errors carry structured payloads (e.g. “validation failed link {id}: cycle detected”).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"files-added-changed","dir":"Articles > Design","previous_headings":"","what":"Files added / changed","title":"Read-only tool layer","text":"R/describe-block.R (new) — describe_block() generic default method block operating registry metadata. R/describe-stack.R (new) — describe_stack() generic default method stack returning short id/name/blocks summary. R/summarise-result.R (new) — summarise_result() generic default method forwarding btw::btw_this(). R/tools-read.R (new) — six tool factories plus register_read_tools(). R/extension.R — one new line calling register_read_tools() chat client built. R/system-prompt.R — default_system_prompt() text revised. DESCRIPTION — add btw Imports:. NAMESPACE — export describe_block, describe_stack, summarise_result plus default methods. inst/examples/populated-board/app.R (new) — dataset_block(\"iris\") → subset_block, assistant extension mounted alongside. tests/testthat/test-describe-block.R (new) — default method synthetic block; override fake block subclass honoured. tests/testthat/test-describe-stack.R (new) — default method synthetic stack; override fake stack subclass (mirroring dag_stack) surfaces list_stacks rows. tests/testthat/test-summarise-result.R (new) — default dispatches btw_this data.frame non-tabular object; per-test S3 method fake class honoured. tests/testthat/test-tools-read.R (new) — one testthat::describe() per tool: happy path fake board, plus failure path (unknown id, empty board).","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"automated-ci-devtoolscheck","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Automated (CI / devtools::check())","title":"Read-only tool layer","text":"six built-tools registered chat client extension-server start, verifiable length(client$get_tools()) inside shiny::testServer() harness. override describe_block.<fake_block_class> reached corresponding block board — proves per-block generic surface. override describe_stack.<fake_stack_class> appears description column list_stacks — proves per-stack generic surface. tool’s R-level implementation tested isolation static board fixture; get_block_result tested via shiny::testServer() needs reactive context. devtools::check() passes 0/0/0. Phase 1 acceptance criteria still pass: ser/des round-trip, streaming, stop button, token telemetry — adding tools must regress conversational behaviour.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"manual-demo-app-live-llm","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Manual (demo app, live LLM)","title":"Read-only tool layer","text":"can’t exercised CI — need real chat backend. gate merge, CI green. Demo app launches without errors default_chat fallback. User asks “blocks board?” → model invokes list_blocks replies actual block ids. User asks “block X ?” → model invokes describe_block replies constructor args current values. User asks “result X look like?” → model invokes get_block_result returns summarise_result() summary.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/2-read-tools.html","id":"known-limitations-carried-into-later-phases","dir":"Articles > Design","previous_headings":"","what":"Known limitations carried into later phases","title":"Read-only tool layer","text":"staging. Every tool reads board$board / board$blocks directly; fine read-tools explicit motivation Phase 3’s pending-update layer. dynamic board context system prompt. model sees board explicitly calls tool. Phase 5 add board summary model can answer trivial “many blocks?” questions without tool round-trip. tool list hard-coded system prompt. Generating register_read_tools() Phase 5 concern. tool-contribution API sibling dock extensions; see #4 upstream prerequisite blockr.dock#129. btw added Imports. lighter alternatives appear cover generic-summary surface, cheap swap.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"goal","dir":"Articles > Design","previous_headings":"","what":"Goal","title":"Staging & dispatch model","text":"Phase 3 adds architectural keystone mutation tools (Phase 4) ride top : per-turn staging layer LLM tool calls board’s update(...) reactive. Tools longer dispatch updates directly — accumulate change pending payload private extension server, validated incrementally stage time flushed single update(...) call assistant’s turn completes. new user-facing tools land phase. Phase 2 read tools keep working unchanged, assistant’s externally visible behaviour identical. ships internal API (stage_block_*, stage_link_*, stage_stack_*, flush_pending) plus wiring attaches chat turn lifecycle. Phase 4 mutation tools thin façades layer.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Staging & dispatch model","text":": pending_update reactiveVal private extension server whose value shaped exactly like update(...) payload (list(blocks = list(add, mod, rm), links = list(...), stacks = list(...))). small stage_* API (one stager per add/mod/rm × blocks/links/ stacks) explicit conflict semantics. stager runs blockr.core’s validator proposed full pending payload live board mutating pending_update; rejections surface with_tool_errors-shaped error strings. flush_pending(pending, update) helper calls update(pending()) something flush, resets reactiveVal. Turn-lifecycle wiring asst_ext_srv: reset pending mod$last_input (new user turn begins), flush mod$last_turn (assistant turn complete). Error formatting evolved Phase 2’s with_tool_errors: stage-time rejection returns string model can self-correct (\"add_block failed: block 'foo' already exists. Use modify_block change existing block.\"). Tests tests/testthat/test-staging.R exercising stage API synthetic board + shiny::testServer() harness. (deferred later phases): new LLM-facing tool. stage API internal; callers Phase 3 tests. External-control writes (block_external_ctrl_vars reactiveVals). Phase 4 decide whether modify_block rides staging layer (replacing block via blocks$mod) hits per-input reactiveVal directly. Either way, Phase 3’s payload-shaped staging primitive independent decision. “pending changes” UI affordance (badge counting staged operations, diff preview, “apply now” button). pending reactiveVal left observable affordance cheap add later, Phase 3 ships none. Snapshotting/undo. Atomicity turn boundary deliver ; multi-turn rollback waits blockr.session hooks don’t yet exist.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"why-staging-at-all","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Why staging at all","title":"Staging & dispatch model","text":"blockr.core’s board server exposes single board_update reactiveVal() shared across plugins, callbacks extensions (blockr.core/R/board-server.R:82). update(payload) call sets reactiveVal; priority-Inf observer validates payload current board, priority--Inf observer applies resets reactiveVal NULL. validate/apply pair runs per flush reactive graph. Two consequences LLM-driven caller: Multiple update() calls inside one reactive flush race. reactiveVal holds one value. model calls two mutation tools issues update(...) synchronously, second call overwrites first; one set changes actually reaches validator/applier pair next reactive flush. ellmer’s tool dispatch runs inside chat_mod_server’s observer chain — tool calls within single assistant turn live reactive epoch collide way. “pending validation” intermediate state. Per-tool feedback (“last call succeed?”) meaningful tool can see -flight state — .e. board plus every prior staged change turn. blockr.core’s validators run rv$board ; don’t know sibling tool’s earlier update() call flushed applied. Staging fixes : tools mutate server-private pending_update reactiveVal whose value accumulated payload. stage runs blockr.core’s validators merged state (current board + pending payload), model gets per-tool feedback grounded next tool actually see. assistant’s turn completes, accumulated payload dispatched one update(...) call — one validator pass, one applier pass, one re-render, one logical snapshot point future blockr.session integration. staging layer central architectural deliverable package; every Phase 4 mutation tool thin façade .","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"pending-payload-shape-mirrors-update----exactly","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Pending payload shape mirrors update(...) exactly","title":"Staging & dispatch model","text":"pending payload shaped identically blockr.core’s update() expects: Two reasons keeping shapes identical: Flush update(pending()). transformation step, shape adapter, risk drift blockr.core’s payload shape evolves. merge helper can reuse blockr.core’s combine_* internals (combine_update_blocks() blockr.core/R/board-server.R:897, plus analogous logic links stacks). Phase 3 re-implement trivial bits -package — upstream helper unexported want avoid ::: dependency — shape compatibility means exporting later -op refactor. initial pending value fully-populated empty-skeleton , NULL. trades one allocation per turn cleaner stage API: stagers never check “blocks$add slot exist yet?” — always , just empty.","code":"list(   blocks = list(     add = blockr.core::blocks(),  # an empty `blocks` typed object     mod = blockr.core::blocks(),     rm  = character()   ),   links  = list(     add = blockr.core::links(),     mod = blockr.core::links(),     rm  = character()   ),   stacks = list(     add = blockr.core::stacks(),     mod = blockr.core::stacks(),     rm  = character()   ) )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"turn-boundaries-stage-on-tool-call-flush-on-last_turn","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Turn boundaries: stage on tool call, flush on last_turn","title":"Staging & dispatch model","text":"shinychat::chat_mod_server() returns module result whose last_input() reactive fires user submits message last_turn() reactive fires assistant finishes turn. Concretely, last_turn written observer keyed append_stream_task$status() == \"success\" (shinychat::chat_mod_server source) — stream task uses client$stream_async(), handles tool roundtrips internally resolving. last_turn fires per assistant turn tool calls run, per intermediate tool-call turn. exactly boundary want flush hook . two events bracket turn: last_input — reset pending. Defensive: previous turn crashed flushing, don’t carry stale staging new turn. Also makes invariant “turns, pending empty” cheap assert. last_turn — flush pending. Build update(...) payload accumulated stagers dispatch. nothing staged, -op. two observers replace Phase 1’s pair (called record_new_turns() ) — staging side-effects now bracket recording call. flush_pending calls update(payload) sets blockr.core’s board_update reactiveVal; blockr.core’s priority-Inf validator priority--Inf applier observers process next reactive flush. observer doesn’t wait — time record_new_turns() runs, dispatch queued, yet applied. ordering fine: chat history goes state$messages, board state lives rv$board, saved bookmark gathers cascade settled. within-observer ordering constraint enforce . consequence: model can’t read back mutations within turn. Phase 3 read tools (unchanged Phase 2) read directly board$board, reflects post-dispatch state — dispatch runs turn completes. list_blocks called turn staged add_block(new_id) see new_id, get_block_result(new_id) returns “block” reason (block server built yet either way). model can ask user nudge next turn, — interactive “build inspect” pattern — split work across two user turns. Whether plumb merged view read tools deliberate Phase 4 decision (see Read tools merge view ). considered three alternatives rejected : Flush model produces final response. ellmer’s tool-call loop opaque outside; hook “last tool call returned” “model emits final text”. viable. Flush per tool call. Defeats staging premise — back racing single-reactiveVal problem issue (1) . Flush quiet idle (e.g. 200ms last tool call). Timing-dependent racy next tool call turn. last_turn clean signal.","code":"observeEvent(   mod$last_input(),   {     reset_pending(pending_update)     record_new_turns()   },   ignoreNULL = TRUE )  observeEvent(   mod$last_turn(),   {     flush_pending(pending_update, update, session)     record_new_turns()   },   ignoreNULL = TRUE )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"stage-time-validation-against-merged-state","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Stage-time validation against merged state","title":"Staging & dispatch model","text":"Every stager re-uses blockr.core’s validators (validate_board_update, plus per-slot helpers) proposed full pending payload current real board. blockr.core’s validators merging internally matters — combine_update_blocks(upd, board) validate_update_links_board feeds link-block cross-reference check, analogous logic gates stack check. never construct “virtual merged board” stage time; just hand validator next-tick view payload live board, trust right thing. validate_pending(payload, board) -package single-entrypoint wrapper around blockr.core’s validator chain (see Validator access reach unexported helpers). stager throws rejection rather returning error value. Phase 2’s with_tool_errors() already wraps every tool body tryCatch translates exceptions error strings, re-using exceptions keeps surface uniform: Phase 4 mutation tool’s body looks roughly like per-tool conflict handling.","code":"stage_block_add <- function(pending, board, id, block) {    cur <- pending()   new <- cur   new$blocks$add <- c(new$blocks$add, set_names(blocks(block), id))    tryCatch(     {       validate_pending(new, isolate(board$board))       pending(new)       invisible(TRUE)     },     error = function(e) {       stop(         format_stage_error(\"add_block\", id, e),         call. = FALSE       )     }   ) } function(id, type, args) {   with_tool_errors(\"add_block\", {     block <- build_block(type, args)              # Phase 4 helper     stage_block_add(pending_update, board, id, block)     sprintf(\"Staged add_block(%s) — will apply at turn end.\", id)   }) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"conflict-semantics-lenient-collapse-reject-only-the-genuinely-ambiguous","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Conflict semantics: lenient collapse, reject only the genuinely ambiguous","title":"Staging & dispatch model","text":"stager step prior pending entry, collapse two operations latest-intent outcome wherever result unambiguous, reject rows combined intent genuinely ill-defined. rule table: Five rows collapse, four reject. collapses cover every case model refining intent flight — typing wrong arg add_block immediately correcting via modify_block, staging add changing mind, swapping mod rm. rejections reserved cases combined intent genuinely ambiguous (rm add reads either “replace” “cancel rm create something new”; model say ) simply impossible (add ID twice; mod something ’s removed). trade-: model can recover mistakes within turn. stricter table, model stages add, -turn path fix — every follow-rejects, bad add flushes turn end, recovery deferred next user turn. ’s dead-end LLM navigate around. lenient table closes trap. flush-time validator safety net. point staging per-call feedback, source truth “payload safe” validate_board_update() flush time. Lenient stage-time collapse can’t produce invalid flush — combined intent expressed collapsed payload structurally bad, dispatch-time validator catches . Collapse semantics unambiguous. Every accepted row single intuitive meaning: latest call wins id. surprise “add mod” produces. table extends links stacks obvious substitutions.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"read-tools-and-the-merge-view-deferred-decision","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Read tools and the merge view (deferred decision)","title":"Staging & dispatch model","text":"Phase 2’s read tools (list_blocks, describe_block, list_links, …) read directly board$board / board$blocks without going merge. Phase 3 lands, tool asks “blocks board?” sees committed board — blocks staged current turn. Two stances defensible: Read board$board. Honest — model sees ’s actually live. Symmetric get_block_result, can’t evaluate pending block anyway (server built yet). Recommended Phase 3. Read merged view applies pending_update top live board. model sees world just building. Lets reason “add link foo bar?” within turn added foo. Phase 3 ships option (1): zero-touch read tools, risk model thinking block exists doesn’t (yet), need ship merge helper Phase 3 caller. Phase 4 revisits mutation tools flight actual question — “model practice need -turn read--write?” — becomes empirical rather hypothetical. Phase 4 lands option (2), introduces merge helper point. ship one Phase 3 deliberately: stage-time validation need (blockr.core’s validator handles necessary merging internally cross-reference checks), Phase 3 helper dead code YAGNI risk -specifying Phase 4 actually wants (e.g. accounting blockr.core’s apply-time auto-cleanups dangling refs).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"atomicity-on-flush-all-or-nothing-no-partial-apply","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Atomicity on flush: all-or-nothing, no partial apply","title":"Staging & dispatch model","text":"update(pending()) errors dispatch — e.g. blockr.core’s validator rejects merged payload board changed turn (user added block via UI now collides pending add) — Phase 3 attempt partial application. entire staged payload discarded, pending reactiveVal reset, user sees Shiny error notification. Rationale: Atomicity feature. turn’s mutations represent model’s collective intent. Half-applying (“blocks went links rolled back cycle”) leaves board state model never asked hard user reason . Stage-time validation already catches structural cases. Flush-time rejection reserved races (concurrent user edit) bugs staging API. Neither right place clever recovery. model can retry next turn. partial apply followed “please continue” prompt confusing recovery “last set changes rejected X; ’s current board, try .” Shiny-level error surface whatever blockr.core’s validator emits — notify(type = \"error\") toast via standard notify_user plugin. customise Phase 3. model sees next turn whatever surface user (future “tell model happened” affordance) chooses expose. Phase 3 makes commitment ; recorded conversation captures model emitted, flush rejection invisible model unless user types back. Phase 5’s prompt-context layer natural home “new turn, prepend system note describing board changes since last turn (including rolled-back flushes)” — scope .","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"external-control-writes-are-not-staged-in-phase-3","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"External-control writes are not staged in Phase 3","title":"Staging & dispatch model","text":"blockr.core’s external_ctrl mechanism lets block expose subset arguments reactiveVals (block_external_ctrl_vars(block) enumerates ), bypassing update() parameter tweaks. natural Phase 4 use modify_block(id, args) writing directly vars target block’s rv$inputs[[id]][[arg_name]]() reactives. path doesn’t intersect update(...) — mutates per-block reactiveVals directly. Phase 3’s staging layer shaped around update(...) payload, external-control writes sit outside construction. Open question Phase 4, deliberately decided : Stage external-control writes ? Pro: single “flush end turn” point keeps reactive cascades re-evaluating block per modify_block call within turn. Con: requires parallel staging structure (per-input values keyed block-id) equivalent update(...) payload. Fire external-control writes immediately? Pro: simpler, matches human user clicking ctrl-block submit button works. Con: reactive cascade per call. Phase 4 pick. Phase 3 keeps staging primitive scoped update(...) payloads decision stays open.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"initial-state","dir":"Articles > Design","previous_headings":"Pending payload shape and stagers","what":"Initial state","title":"Staging & dispatch model","text":"reset_pending(pending_update) sets pending_update(empty_pending()).","code":"empty_pending <- function() {   list(     blocks = list(       add = blockr.core::blocks(),       mod = blockr.core::blocks(),       rm  = character()     ),     links = list(       add = blockr.core::links(),       mod = blockr.core::links(),       rm  = character()     ),     stacks = list(       add = blockr.core::stacks(),       mod = blockr.core::stacks(),       rm  = character()     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"stager-signatures","dir":"Articles > Design","previous_headings":"Pending payload shape and stagers","what":"Stager signatures","title":"Staging & dispatch model","text":"stagers take pending_update reactiveVal, live board reactive (merge-time validation), per-operation arguments: one: Reads current pending payload (isolate(pending())). Applies conflict-resolution rule op prior pending entry id (see Conflict semantics ). Five rows collapse — add → mod replaces pending add’s value, add → rm drops pending add, mod → mod last-write-wins, mod → rm discards pending mod, plus natural identity collapses; four rows throw format_stage_error(op, id, msg) model-actionable explanation. Computes proposed-new pending payload. Calls validate_pending(proposed, isolate(board$board)), wraps blockr.core’s validator chain. Catches blockr_abort conditions re-throws via format_stage_error. blockr.core’s validator handles “combined update vs current board” logic (including merging adds block list validating link references) — see Stage-time validation . Sets pending(proposed) success returns invisible(TRUE).","code":"stage_block_add(pending, board, id, block) stage_block_mod(pending, board, id, block) stage_block_rm(pending, board, id)  stage_link_add(pending, board, id, link) stage_link_mod(pending, board, id, link) stage_link_rm(pending, board, id)  stage_stack_add(pending, board, id, stack) stage_stack_mod(pending, board, id, stack) stage_stack_rm(pending, board, id)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"flush_pendingpending-update","dir":"Articles > Design","previous_headings":"Pending payload shape and stagers","what":"flush_pending(pending, update)","title":"Staging & dispatch model","text":"Note: blockr.core’s preprocess_board_update may extend payload dispatch time (adding link-removals orphaned links blocks removed, similar stack cleanups — blockr.core/R/board-server.R:1020). applied set may therefore superset staged. correct behaviour — cleanup user expect — means stage adds blocks , B, C removes B may dispatch “add , C; remove B; remove dangling-link-x”. visible model, since didn’t stage link removal, worth flagging anyone debugging flush. has_any_changes one-liner lengths() six add/mod/rm slots. tryCatch keeps malformed payload crashing chat module — Shiny error notification blockr.core’s validator already surfaces user. finally = reset_pending clause guarantees never carry rejected payload next turn.","code":"flush_pending <- function(pending, update) {    payload <- isolate(pending())    if (!has_any_changes(payload)) {     reset_pending(pending)     return(invisible(FALSE))   }    tryCatch(     update(payload),     error = function(e) {       log_warn(\"flush_pending: dispatch rejected payload: \",                conditionMessage(e))     },     finally = reset_pending(pending)   )    invisible(TRUE) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"error-formatting","dir":"Articles > Design","previous_headings":"Pending payload shape and stagers","what":"Error formatting","title":"Staging & dispatch model","text":"Identical shape Phase 2’s with_tool_errors, model sees \"<tool> failed: <reason>\" pattern whether failure came inside tool body stage-time conflict. reject logged warning — ’s expected flow LLM still feeling board. flush-time rejection previous section logged, point model believed stage succeeded dispatch-time failure genuinely surprising.","code":"format_stage_error <- function(op, id, e) {    msg <- if (inherits(e, \"condition\")) conditionMessage(e) else e    sprintf(     \"%s(%s) failed: %s\",     op,     id,     msg   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"extension-server-diff","dir":"Articles > Design","previous_headings":"","what":"Extension server diff","title":"Staging & dispatch model","text":"Phase 2 observers mod$last_input / mod$last_turn called record_new_turns() directly subsumed new observers. record_new_turns() keeps behaviour — appends new turns recorded messages reactive — now shares turn-boundary observer staging side-effects. pending_update deliberately added state. ’s transient turn-local scratch space, part persisted extension contract; carrying ser/des let half-staged turn restore inconsistent state. invariant “turns, pending empty” makes “sessions, pending empty” trivially true. register_read_tools() also touched. read tools Phase 3 reason see pending_update (continue read live board). Phase 4 lands merged-view option read tools, ’s signature change happens — Phase 3 deliberately avoids churn may turn unneeded.","code":"asst_ext_srv <- function(system_prompt, messages) {    function(id, board, update, ...) {      moduleServer(       id,       function(input, output, session) {          chat_ctor <- isolate(get_board_option_value(\"llm_model\", session))         sys_prompt <- coal(system_prompt, default_system_prompt())          client <- chat_ctor(system_prompt = sys_prompt)          pending_update <- reactiveVal(empty_pending())             # new          register_read_tools(client, board, update, session)        # unchanged from Phase 2          if (length(messages)) {           client$set_turns(lapply(messages, ellmer::contents_replay))         }          mod <- shinychat::chat_mod_server(\"chat\", client)          # … existing record_new_turns + messages reactive …          observeEvent(                                               # changed           mod$last_input(),           {             reset_pending(pending_update)             record_new_turns()           },           ignoreNULL = TRUE         )          observeEvent(                                               # changed           mod$last_turn(),           {             flush_pending(pending_update, update)             record_new_turns()           },           ignoreNULL = TRUE         )          # token telemetry unchanged          list(           state = list(             system_prompt = sys_prompt,             messages      = messages           )         )       }     )   } }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"files-added-changed","dir":"Articles > Design","previous_headings":"","what":"Files added / changed","title":"Staging & dispatch model","text":"R/staging.R (new) — empty_pending(), reset_pending(), validate_pending(), nine stage_* functions, flush_pending(), format_stage_error(), has_any_changes(). R/extension.R — wire pending_update server swap two record--turn-boundary observers new reset--record / flush--record observers. tests/testthat/test-staging.R (new) — covers stager’s happy path, conflict-rule rejection, flush helper (including empty-pending -op validator-rejection branch), turn-boundary wiring via shiny::testServer() fake board. tests/testthat/test-extension.R — extend turn-boundary test: stage three operations across two fake tool calls, trigger last_turn, assert exactly one update(...) call fired pending reset. new exports. stage API internal — Phase 4 decide whether pieces deserve exporting mutation tools shake actual extensibility needs. new dependencies. new vignette (design doc deliverable Phase 3; user-facing tutorial Phase 5 concern).","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"automated-ci-devtoolscheck","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Automated (CI / devtools::check())","title":"Staging & dispatch model","text":"nine stage_* functions happy-path test synthetic board fixture, plus one test per row conflict-resolution table — collapse rows assert collapsed pending payload, rejection rows assert expected error string. Empty pending → update(...) call, returns FALSE. Non-empty pending → exactly one update(...) call accumulated payload, pending reset empty_pending(), returns TRUE. update(...) throws → caller crash, pending still reset, warning logged. mod$last_input firing resets non-empty pending. mod$last_turn firing flushes pending exactly . Two last_turn firings without intervening stages produce zero update(...) calls. Phase 1 Phase 2 acceptance criteria still pass — staging additive, read tool chat behaviour regresses. devtools::check() passes 0/0/0.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"manual-demo-app-live-llm","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Manual (demo app, live LLM)","title":"Staging & dispatch model","text":"Phase 2 demo (inst/examples/populated-board/) still runs end--end: read tools answer “blocks?” etc. correctly, update(...) ever dispatched, pending stays empty every turn. regression smoke test — Phase 3 observationally invisible user without mutation tools. new manual criteria; staging user-visible surface Phase 4.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/3-staging.html","id":"known-limitations-carried-into-later-phases","dir":"Articles > Design","previous_headings":"","what":"Known limitations carried into later phases","title":"Staging & dispatch model","text":"-turn read--write. Tools read committed board, merged view. empirical Phase 4 usage shows model wants inspect just staged, Phase 4 introduces merge helper wires read-tool factories — see Read tools merge view (deferred decision). Flush rejection silent model. user sees Shiny error notification; model sees nothing next turn unless user types error back. Phase 5’s prompt-context layer natural home “tell model state changes since last turn”. “pending changes” UI affordance. badge (“3 staged changes apply assistant finishes”) diff preview helpful trust; reactiveVal left observable cheap bolt , Phase 3 ships nothing. External-control writes staged. Phase 4 mutation tools hit block_external_ctrl_vars fire reactive cascades per call within turn. turns painful, Phase 4 (follow-) adds parallel per-input staging structure — orthogonal phase’s payload-shaped staging. Atomic flush partial-success fallback. racing user edit invalidates payload dispatch time discards whole turn’s mutations. design; trade-bites practice, future phase can add “apply can” mode behind constructor option. snapshot / undo. flush fresh update(...) rollback handle. Snapshot integration waits blockr.session hooks don’t yet exist.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"goal","dir":"Articles > Design","previous_headings":"","what":"Goal","title":"Mutation tools","text":"Phase 4 turns Phase 3 staging primitive usable surface LLM: nine mutation tools (add_block, remove_block, modify_block, add_link, remove_link, modify_link, add_stack, remove_stack, modify_stack) assistant can call build edit board. Every tool thin façade Phase 3 stage API — body constructs relevant object (forwards delta), calls stage_* function, returns short result string. new dispatch path, parallel reactive structure visible model: every mutation accumulates pending_update reactiveVal flushes via existing flush_pending hook last_turn. phase deliberately pushes routing complexity blockr.core. modify_block call ships single partial-args delta part update(...) payload; core decides whether service external-control write atomic re-setup block server (see blockr.core#175). consumer sees one shape branch.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Mutation tools","text":": Nine mutation tools, registered alongside Phase 2 read tools via new register_mutation_tools() helper. refactor Phase 3’s stage_block_mod(), stage_stack_mod() stage_link_mod() accept partial-args delta rather full constructed object — matching unified dispatch shape agreed blockr.core#175. inst/examples/empty-board/ manual smoke-testing live LLM. Tests covering tool’s happy path, new delta-mod conflict rows, ID-generation behaviour, turn-boundary end--end test via shiny::testServer(). (deferred later phases roadmap): -turn read--write. Phase 2 read tools continue read committed board . Staged operations visible model tool-call history. Revisited Phase 5 telemetry shows model confused. “pending changes” UI affordance (badge, diff preview, apply-now button). reactiveVals observable; extension cheap bolt later. Snapshot / undo. Atomicity turn boundary ship; multi-turn rollback waits blockr.session hooks yet exist. Per-block-type custom tools. Block packages contributing tools (set_filter_expression, etc.) separate roadmap item. Multi-board / multi-session orchestration. Extension state scoped board mounted . Phase 4 gated blockr.core change tracked blockr.core#175: unified delta-shape mod slots three entity types (blocks, stacks, links), blocks additionally routed internally external-control writes vs. atomic re-setup. lands, Phase 4 pins blockr.core via Remotes: branch carrying fix. design proceeds unified dispatch already available.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"trust-core-to-dispatch-do-not-split-routing-in-the-consumer","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Trust core to dispatch; do not split routing in the consumer","title":"Mutation tools","text":"natural shape modify_block(id, args) one tool ships one update(...) payload — trusts framework right thing. Today (pre-#175) framework forces consumers choose: write external-control reactiveVals directly (bypassing update() entirely, fast per-call cascade), stage blocks$mod entry won’t actually re-run server. Splitting choice every consumer mutation API leaks implementation detail blockr.core every LLM-driven (script-driven) board mutator. Phase 4 takes opposite stance. modify_block stages exactly one thing — partial-args delta pending_update$blocks$mod[[id]] — flush_pending ships via update(...). core routing table (ctrl write changed args ctrl-able live block; atomic re-setup otherwise) core’s problem. Two consequences shape rest document: pending_ctrl reactiveVal. Phase 3 left door open parallel staging structure ctrl writes (“turns painful”). Phase 4 walks door back : staging structure whole tool surface update(...)-shaped pending_update Phase 3. sibling reactiveVals. model sees uniform semantics. Every mutation tool returns \"Staged <tool>(<id>) — apply turn end.\". tool fires side-effects mid-turn. whole assistant turn one atomic board update.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"mods-are-deltas-adds-are-full-constructions","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Mods are deltas; adds are full constructions","title":"Mutation tools","text":"Mutation tools split two shapes: add_block, add_link, add_stack carry full set constructor arguments produce typed object (blockr.core::create_block(type, ...), new_link(...), new_stack(...)). constructor call natural place surface “missing required arg” / “wrong type” errors model, routed with_tool_errors <tool> failed: <reason> string. Construction--validation: construct object stage anyway, validation free. modify_block, modify_stack, modify_link carry partial-args delta (named list just keys changed). construction consumer. delta lands pending_update$<entity>$mod[[id]] flushes part update(...) payload. Core merges delta live entry, validates, dispatches. asymmetry? add knows everything (’s bringing new object existence); mod knows diff. blocks stacks, current args live inside opaque object — blockr.core export block_args() readback, stack subclasses (e.g. blockr.dock::dock_stack color attribute) make generic readback brittle. Forcing consumer reconstruct full object one-arg change require either generic arg-readback API stateful mirror every entity’s args inside extension. Neither worth cost core can merge one place. Links subclassable today support full-replace consumer side, Phase 4 puts delta path API symmetry — every modify_* tool body shape, every mod slot payload shape. means stage-time validation mods shallow — consumer can check delta named list. substantive validation (n_rows exist head_block? proposed value right type?) runs flush live entry, core. cost: typo’d arg name modify_block goes undetected turn end. benefit: don’t ship parallel schema layer consumer ’s guaranteed drift blockr.core’s actual constructor signatures.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"block-ids-are-immutable-for-committed-blocks","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Block ids are immutable for committed blocks","title":"Mutation tools","text":"assistant change id block already exists board. change_id tool, embedded id-change arg modify_block, path kind. links stacks. reasoning: Ids system identifiers, user-facing labels. Blocks carry id (registry key) per-type display name registered via register_block(ctor, name, ...). display name type-level metadata, per-instance editable surface; “rename block” user request maps onto changing rendered label board UI (scope Phase 4) picking different block type, changing registry key. conflation “name” “id” casual speech makes worth spelling . Stable ids = stable model context. model rebuilding mental model board across turns list_blocks / describe_block output. ids drift mid-conversation, model’s tool-call history current board state desynchronise. External references stay valid. Saved app state, bookmarks, upstream observers key block id, future blockr.session integration assume committed ids stable. assistant’s mutation surface thing breaks . staged (yet committed) block, id freely changeable — via dedicated tool. model just stages remove_block(old_id) followed add_block(<type>, <args>, id = new_id). Behaviorally identical re-keying pending entry, costs two tool calls instead one, avoids tool detect “id pending vs. committed?” dispatch differently. user asks assistant change committed block’s id, expected handling conversational: assistant explains can offer remove_block(old_id) + add_block(<type>, <args>, id = new_id), tears block server (lost eval state, re-evaluation downstream blocks), asks user whether ’s acceptable. Phase 5’s prompt-context layer likely codify kind guidance system prompt. rule applies link stack ids. conflict table id-change rows entity type.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"hybrid-id-generation-via-blockr-corerand_names","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Hybrid ID generation via blockr.core::rand_names()","title":"Mutation tools","text":"add_block, add_link, add_stack accept optional id argument: model supplies id, used -. Uniqueness merged board-plus-pending state checked core’s validator stage time (Phase 3’s validate_pending already runs full validator chain). Format constraints whatever core enforces — layer . model omits id, package generates one via blockr.core::rand_names(old_names = existing_ids(board, pending)). Core’s default rand_names produces adjective-animal ids (quick-elephant, tidy-otter) via optional ids package, falling back random letter strings. tool result string echoes final id either path. Result string template: id always present unambiguous, model can quote subsequent calls without needing separate listing tool.","code":"\"Staged add_block(quick-elephant) — will apply at turn end.\""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"read-tools-stay-committed-only-reaffirmed","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Read tools stay committed-only (reaffirmed)","title":"Mutation tools","text":"Phase 3’s deferred decision settled favour option (1): read tools read board$board, merged board-plus-pending view. reasons accumulated since Phase 3 still hold: stage-time validator already accounts pending state checking cross-references (e.g. add_link(foo→bar) staged add_block(foo) validates merged payload thanks validate_pending calling combine_update_blocks core). get_block_result return value pending block (server built yet), partial merge view create illusion usable block. model’s tool-call history records staged turn — separate “list pending” surface adds nothing conversation log already carry. Phase 5’s user-test telemetry surfaces friction , lightweight fix new list_pending_changes() tool explicitly reports staged operations (retrofit existing read tools).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"validity-checks-delegated-to-core","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Validity checks delegated to core","title":"Mutation tools","text":"Phase 3’s validate_pending(payload, board) runs full validate_board_update chain every stage. Cycles, dangling block references links, missing target inputs, duplicate ids rest caught merged state. Phase 4 adds nothing new. stagers stay thin: collapse conflict table, call commit_pending, return. consumer-side re-implementation cross-reference rule.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"reject-add-mod-recover-via-remove-add","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Reject add → mod; recover via remove + add","title":"Mutation tools","text":"Phase 3’s conflict table lenient-collapses add(x) followed mod(x): pending add’s value replaced mod’s new value. Phase 3 mod carried full block object, collapse trivial swap. Phase 4 mod carries delta, producing collapsed value require rebuilding staged block merged args via create_block(type, merged_args). original (type, args) recipe known stage time add_block call, discarded block stored — public arg readback constructed block. maintain sidecar recipe cache (second reactiveVal keyed id holding list(type = ..., args = ...) every staged add). close gap keep row collapse. cost second reactiveVal stay lockstep pending_update across every add rm, plus complex stage_block_mod branches “live-board target pending-add target?”. Phase 4 takes simpler trade. add → mod row rejects. model recovers issuing remove_block(x) + add_block(type, corrected_args, id = \"x\") instead. corrected args visible model tool-call history (prior add_block call sits conversation log original type args), reconstructing knowledge problem. rejection error string points model recovery path explicitly: Cost: extra tokens model self-corrects pending-add mid-turn, especially blocks many args. frequency bounded (triggers -turn second-thoughts add). Benefit: second reactiveVal, recipe bookkeeping, divergence risk staged block recipe. rule stacks: add_stack(x) → modify_stack(x, ...) rejects. Base stacks expose stack_blocks() / stack_name() readers, subclasses blockr.dock::dock_stack add extra attributes (e.g. color) without generic readback API, recipe-free collapse portable across subclasses. Keeping rule uniform across block stack mods also easier model internalise two different behaviours two superficially-identical operations. one collapse doesn’t need reconstruction still table: add(x) → rm(x) drops pending add. trivial named-list manipulation, recipe required.","code":"\"modify_block(foo) failed: foo is staged for creation this turn.  Use remove_block(foo) followed by add_block(<type>, <args>, id = \\\"foo\\\")  with the corrected arguments.\""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"conflict-table-for-delta-mods","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Conflict table for delta-mods","title":"Mutation tools","text":"Phase 3’s nine-row table blocks extends one row flip: add → mod becomes reject (see previous section) rather collapse. remaining collapses keep Phase 3 mechanics except mod → mod, collapse now merges deltas key-wise rather replacing full block. blocks ({add, mod, rm} × {add, mod, rm}): merge_args(prev, new) helper named-overwrite: keys new replace prev; keys prev present new survive. Matches “delta means partial override”. Stacks links follow table, substituting appropriate modify_* tool name. three entity types identical conflict shape now links also surface modify_* tool delta semantics. Principle inherited Phase 3: collapse combined intent unambiguous cheap, reject otherwise (including one Phase 4 reject — add → mod — buys us recipe cache).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"tool-result-formatting","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Tool result formatting","title":"Mutation tools","text":"nine tools follow template: shape Phase 3’s format_stage_error failures (\"<tool>(<id>) failed: <reason>\") model parses one consistent pattern across whole API. structured tool result (JSON object). model consumes chat content tool result; single sentence enough confirm call landed recall id later. Phase 5 may revisit richer telemetry helps model self-correct.","code":"\"Staged <tool>(<id>) — will apply at turn end.\""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"atomicity-errors-and-flush-rejection","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Atomicity, errors, and flush rejection","title":"Mutation tools","text":"Inherited Phase 3 unchanged: turn’s mutations atomic. stage error throws tool body, gets re-shaped with_tool_errors, goes back model <tool> failed: <reason> string. pending payload mutated failing stage — model can retry. Flush rejection (validator throws update(...) dispatch time) discards whole staged payload logs warning. user sees standard Shiny notification blockr.core’s validator; model sees nothing next turn unless user (Phase 5’s prompt-context layer) tells . partial apply flush. Phase 4 changes nothing .","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"tool-surface","dir":"Articles > Design","previous_headings":"","what":"Tool surface","title":"Mutation tools","text":"tool factory Phase 2 shape: tool_<name>(board, update, pending, session) returns ellmer::tool(...). Bodies elide with_tool_errors(\"<name>\", { ... }) wrapper brevity; assume tool body wrapped exactly like Phase 2 read tools R/tools-read.R.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"add_blocktype-args-id-null","dir":"Articles > Design","previous_headings":"Tool surface","what":"add_block(type, args, id = NULL)","title":"Mutation tools","text":"args ships JSON-formatted string rather native object ellmer’s type_object requires closed key list escape hatch (.additional_properties = TRUE) deprecated supported Claude. JSON-string parameter provider-agnostic; body parses jsonlite::fromJSON surfaces parse failures, construction failures (missing required arg, wrong type, unknown type id) <tool> failed: <reason> envelope. stage_block_add enforces uniqueness board + pending via core’s validator conflict-table reject rows.","code":"tool_add_block <- function(board, update, pending, session) {    ellmer::tool(     function(type, args, id = NULL) {        id <- id %||% rand_names(existing_ids(board, pending, \"blocks\"))        parsed <- jsonlite::fromJSON(args, simplifyVector = TRUE)       block <- do.call(create_block, c(list(type), parsed))        stage_block_add(pending, board, id, block)        sprintf(         \"Staged add_block(%s) — will apply at turn end.\", id       )     },     name        = \"add_block\",     description = paste(       \"Add a new block to the board. `type` is a block id as\",       \"reported by list_available_blocks. `args` is a JSON object\",       \"(passed as a string) of constructor arguments — field names\",       \"must match the arg names reported by list_available_blocks\",       \"for the chosen type. `id` is optional — if omitted,\",       \"a unique id is generated.\"     ),     arguments = list(       type = ellmer::type_string(         \"Block type id, from list_available_blocks.\"       ),       args = ellmer::type_string(         paste(           \"JSON object of constructor arguments, e.g.\",           \"'{\\\"n\\\": 10}'. Field names match list_available_blocks.\"         )       ),       id = ellmer::type_string(         \"Optional id for the new block. Generated if omitted.\",         required = FALSE       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"remove_blockid","dir":"Articles > Design","previous_headings":"Tool surface","what":"remove_block(id)","title":"Mutation tools","text":"cascade-cleanup orphaned links stack memberships core’s job preprocess_board_update time. tool stage ; model need know happen.","code":"tool_remove_block <- function(board, update, pending, session) {    ellmer::tool(     function(id) {        stage_block_rm(pending, board, id)        sprintf(         \"Staged remove_block(%s) — will apply at turn end.\", id       )     },     name        = \"remove_block\",     description = paste(       \"Remove a block from the board. Any links to or from the\",       \"block are cleaned up at apply time by core; the model does\",       \"not need to remove them explicitly.\"     ),     arguments = list(       id = ellmer::type_string(\"Block id to remove.\")     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"modify_blockid-args","dir":"Articles > Design","previous_headings":"Tool surface","what":"modify_block(id, args)","title":"Mutation tools","text":"JSON-string treatment add_block reason — ellmer model open-keyed object portably. stage_block_mod writes parsed delta pending$blocks$mod[[id]], collapses prior pending mod key-wise merge, rejects pending add / rm per conflict table. construction consumer.","code":"tool_modify_block <- function(board, update, pending, session) {    ellmer::tool(     function(id, args) {        delta <- jsonlite::fromJSON(args, simplifyVector = TRUE)        stage_block_mod(pending, board, id, delta)        sprintf(         \"Staged modify_block(%s) — will apply at turn end.\", id       )     },     name        = \"modify_block\",     description = paste(       \"Change one or more constructor arguments of an existing\",       \"block. `args` is a JSON object (passed as a string) of\",       \"just the keys being changed; unmentioned keys keep their\",       \"current values.\"     ),     arguments = list(       id = ellmer::type_string(\"Id of the block to modify.\"),       args = ellmer::type_string(         paste(           \"JSON object of arguments to change, e.g.\",           \"'{\\\"n\\\": 5}'. Only supplied keys are modified.\"         )       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"add_linkfrom-to-input-id-null","dir":"Articles > Design","previous_headings":"Tool surface","what":"add_link(from, to, input, id = NULL)","title":"Mutation tools","text":"Cross-reference validity (blocks exist, input valid slot , cycle introduced) checked core’s validator stage time merged board-plus-pending state.","code":"tool_add_link <- function(board, update, pending, session) {    ellmer::tool(     function(from, to, input, id = NULL) {        id <- id %||% rand_names(existing_ids(board, pending, \"links\"))        link <- new_link(from, to, input)        stage_link_add(pending, board, id, link)        sprintf(         \"Staged add_link(%s: %s → %s$%s) — will apply at turn end.\",         id, from, to, input       )     },     name        = \"add_link\",     description = paste(       \"Add a link that wires the output of block `from` into\",       \"argument `input` of block `to`. Both blocks must exist on\",       \"the board or be staged for creation in this turn.\"     ),     arguments = list(       from  = ellmer::type_string(\"Source block id.\"),       to    = ellmer::type_string(\"Destination block id.\"),       input = ellmer::type_string(         \"Input slot on the destination block.\"       ),       id = ellmer::type_string(         \"Optional id for the new link. Generated if omitted.\",         required = FALSE       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"remove_linkid","dir":"Articles > Design","previous_headings":"Tool surface","what":"remove_link(id)","title":"Mutation tools","text":"","code":"tool_remove_link <- function(board, update, pending, session) {    ellmer::tool(     function(id) {        stage_link_rm(pending, board, id)        sprintf(         \"Staged remove_link(%s) — will apply at turn end.\", id       )     },     name        = \"remove_link\",     description = \"Remove a link by its id.\",     arguments = list(       id = ellmer::type_string(\"Link id to remove.\")     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"modify_linkid-from-null-to-null-input-null","dir":"Articles > Design","previous_headings":"Tool surface","what":"modify_link(id, from = NULL, to = NULL, input = NULL)","title":"Mutation tools","text":"","code":"tool_modify_link <- function(board, update, pending, session) {    ellmer::tool(     function(id, from = NULL, to = NULL, input = NULL) {        delta <- compact(list(from = from, to = to, input = input))        stage_link_mod(pending, board, id, delta)        sprintf(         \"Staged modify_link(%s) — will apply at turn end.\", id       )     },     name        = \"modify_link\",     description = paste(       \"Retarget an existing link. Any combination of `from`, `to`,\",       \"and `input` may be supplied; only supplied fields are\",       \"changed. Omitted fields keep their current values.\"     ),     arguments = list(       id = ellmer::type_string(\"Id of the link to modify.\"),       from = ellmer::type_string(         \"New source block id.\",         required = FALSE       ),       to = ellmer::type_string(         \"New destination block id.\",         required = FALSE       ),       input = ellmer::type_string(         \"New input slot on the destination block.\",         required = FALSE       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"add_stackblocks-name-null-id-null","dir":"Articles > Design","previous_headings":"Tool surface","what":"add_stack(blocks, name = NULL, id = NULL)","title":"Mutation tools","text":"","code":"tool_add_stack <- function(board, update, pending, session) {    ellmer::tool(     function(blocks, name = NULL, id = NULL) {        id <- id %||% rand_names(existing_ids(board, pending, \"stacks\"))        stack <- new_stack(blocks = blocks, name = name)        stage_stack_add(pending, board, id, stack)        sprintf(         \"Staged add_stack(%s) — will apply at turn end.\", id       )     },     name        = \"add_stack\",     description = paste(       \"Group a set of blocks into a stack. `blocks` is a\",       \"character vector of block ids; `name` is an optional\",       \"human-readable label.\"     ),     arguments = list(       blocks = ellmer::type_array(         \"Block ids to include in the stack.\",         items = ellmer::type_string()       ),       name = ellmer::type_string(         \"Optional display name for the stack.\",         required = FALSE       ),       id = ellmer::type_string(         \"Optional id for the new stack. Generated if omitted.\",         required = FALSE       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"remove_stackid","dir":"Articles > Design","previous_headings":"Tool surface","what":"remove_stack(id)","title":"Mutation tools","text":"","code":"tool_remove_stack <- function(board, update, pending, session) {    ellmer::tool(     function(id) {        stage_stack_rm(pending, board, id)        sprintf(         \"Staged remove_stack(%s) — will apply at turn end.\", id       )     },     name        = \"remove_stack\",     description = paste(       \"Remove a stack. Member blocks are not removed; only the\",       \"grouping disappears.\"     ),     arguments = list(       id = ellmer::type_string(\"Stack id to remove.\")     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"modify_stackid-blocks-null-name-null","dir":"Articles > Design","previous_headings":"Tool surface","what":"modify_stack(id, blocks = NULL, name = NULL)","title":"Mutation tools","text":"compact() small utility strips NULL entries list — modify_stack call supplies name produces delta list(name = ...) blocks key.","code":"tool_modify_stack <- function(board, update, pending, session) {    ellmer::tool(     function(id, blocks = NULL, name = NULL) {        delta <- compact(list(blocks = blocks, name = name))        stage_stack_mod(pending, board, id, delta)        sprintf(         \"Staged modify_stack(%s) — will apply at turn end.\", id       )     },     name        = \"modify_stack\",     description = paste(       \"Change a stack's member blocks and/or name. Either or both\",       \"arguments may be omitted; only supplied fields are\",       \"changed.\"     ),     arguments = list(       id = ellmer::type_string(\"Stack id to modify.\"),       blocks = ellmer::type_array(         \"New set of member block ids.\",         items    = ellmer::type_string(),         required = FALSE       ),       name = ellmer::type_string(         \"New display name.\",         required = FALSE       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"stage-api-changes-from-phase-3","dir":"Articles > Design","previous_headings":"","what":"Stage API changes from Phase 3","title":"Mutation tools","text":"Phase 3 stagers take full constructed objects mods. unified delta shape, three mod stagers change signature. new stagers. internal conflict-handling stage_block_mod becomes: Two structural notes: type pending$blocks$mod diverges Phase 3’s typed blocks(...) object. Phase 4 stores named list deltas (named list args), matching post-#175 payload shape. validator commit_pending runs new shape — requires matching change core (#175) land test suite can pass end--end. merge_args helper named-overwrite: merge_args(d1, d2) returns list whose keys union, values d2 winning conflict. stage_stack_mod stage_link_mod follow template entity-specific slot names conflict-table guards. three mod stagers identical body shape; entity key differs (blocks, stacks, links).","code":"stage_block_mod <- function(pending, board, id, delta) {    cur <- isolate(pending())    if (id %in% names(cur$blocks$add)) {     stage_abort(       \"modify_block\", id,       paste0(         id, \" is staged for creation this turn. Use remove_block(\",         id, \") followed by add_block(<type>, <args>, id = \\\"\", id,         \"\\\") with the corrected arguments.\"       )     )   }    if (id %in% cur$blocks$rm) {     stage_abort(       \"modify_block\", id,       \"id is staged for removal; mod has no effect\"     )   }    new <- cur    if (id %in% names(cur$blocks$mod)) {      new$blocks$mod[[id]] <- merge_args(cur$blocks$mod[[id]], delta)    } else {      new$blocks$mod <- c(       new$blocks$mod,       set_names(list(delta), id)     )   }    commit_pending(pending, isolate(board$board), new, \"modify_block\", id) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"extension-server-diff","dir":"Articles > Design","previous_headings":"","what":"Extension server diff","title":"Mutation tools","text":"second reactiveVal beyond Phase 3’s pending_update. extension carries exactly one piece turn-local state staging, mirroring Phase 3 unchanged.","code":"asst_ext_srv <- function(system_prompt, messages) {    function(id, board, update, ...) {      moduleServer(       id,       function(input, output, session) {          chat_ctor <- isolate(           get_board_option_value(\"llm_model\", session)         )         sys_prompt <- coal(system_prompt, default_system_prompt())          client <- chat_ctor(system_prompt = sys_prompt)          pending_update <- reactiveVal(empty_pending())          register_read_tools(client, board, update, session)         register_mutation_tools(                                    # new           client, board, pending_update, session         )          if (length(messages)) {           client$set_turns(             lapply(messages, ellmer::contents_replay)           )         }          mod <- shinychat::chat_mod_server(\"chat\", client)          observeEvent(           mod$last_input(),           {             reset_pending(pending_update)             record_new_turns()           },           ignoreNULL = TRUE         )          observeEvent(           mod$last_turn(),           {             flush_pending(pending_update, update)             record_new_turns()           },           ignoreNULL = TRUE         )          list(           state = list(             system_prompt = sys_prompt,             messages      = messages           )         )       }     )   } }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"files-added-changed","dir":"Articles > Design","previous_headings":"","what":"Files added / changed","title":"Mutation tools","text":"R/tools-mutation.R (new) — nine tool_* factories, register_mutation_tools() helper, existing_ids(), merge_args(), compact(). R/staging.R — refactor stage_block_mod, stage_stack_mod stage_link_mod take delta (instead full block / stack / link). Update empty_pending() three mod slots plain list() (named lists deltas) instead Phase 3’s typed blocks() / links() / stacks() — typed slots longer fit delta shape. R/extension.R — call register_mutation_tools() asst_ext_srv. new reactiveVal. tests/testthat/test-tools-mutation.R (new) — per-tool happy path; per-tool error path (construction failure adds, conflict-table rejects); ID generation (model-supplied, omitted, collision). tests/testthat/test-staging.R — extend new delta shape stage_block_mod / stage_stack_mod / stage_link_mod; remove Phase 3 tests validate full-object mod shape. tests/testthat/test-extension.R — end--end turn-boundary test asserting one update(...) per turn carrying expected shape multi-tool turn (add, link, mod). inst/examples/empty-board/ (new) — minimal demo app exercising full surface. DESCRIPTION — bump Remotes: pin blockr.core branch carrying #175 fix; unpinning release time. Add jsonlite Imports: (used add_block modify_block tool bodies parse JSON-shaped args parameter; tools take typed top-level args need ). jsonlite already loaded transitively via ellmer, explicit declaration documents direct use. new exports. mutation tools internal extension’s server scope, accessed via register_mutation_tools.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"automated-ci-devtoolscheck","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Automated (CI / devtools::check())","title":"Mutation tools","text":"end--end staging / flush assertions gated blockr.core change issue 175. lands, tests pin blockr.core via Remotes: branch carrying delta-shape mod dispatch; CI tracks branch. Per-tool happy path: nine tools, called synthetic board fixture, stages expected entry pending_update returns expected result string. Per-tool error path: least one error case per tool, asserting returned string starts \"<tool> failed: \". Coverage includes construction failure (missing required arg add_*), conflict-table rejects (duplicate add, add → mod, anything staged-rm id), unknown id rm / mod. ID generation: explicit valid id → used -; explicit colliding id → rejected via core’s validator; omitted id → rand_names generates non-colliding id tool result echoes . Conflict-table coverage: one test per collapse row asserting post-stage pending_update state; one test per reject row asserting error string state changed. flush_pending test cases Phase 3 continue pass new payload shape (delta-mods). turn add_block, add_link, add_stack flushes exactly one update(...) carrying three. turn add_block(foo, ...) modify_block(foo, ...) returns failure string modify, flushed update(...) carries original add (mod entry foo). recovery sequence flushes correctly: turn scripted add_block(foo, type, args1) → modify_block(foo, args2) (fails) → remove_block(foo) (collapses pending add -op) → add_block(foo, type, args3) flushes exactly one update(...) carrying single add foo args3 rm. Two last_turn firings without intervening stages produce zero update(...) calls. Phase 1, 2, 3 acceptance criteria continue pass. devtools::check() passes 0/0/0.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"manual-demo-app-live-llm","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Manual (demo app, live LLM)","title":"Mutation tools","text":"new example app inst/examples/empty-board/: empty board, single text input user prompt, pre-loaded state. Used smoke-test full surface end--end real provider. Scenarios verify hand: Build pipeline. “Load iris dataset, drop species column, plot sepal length vs. width.” Expected: turn end board carries 3 blocks (e.g. dataset_block, subset_block, scatter_block), 2 links connecting , 1-message chat transcript reporting staged operation. console errors. Modify place. Starting pipeline : “change dataset mtcars.” Expected: modify_block routes core’s ctrl path dataset arg ctrl-able, atomic re-setup otherwise. Downstream blocks re-evaluate new data; chat reports staged modify . Id-change request user. “Rename dataset data_source.” Expected: assistant declines id change committed block, explains can offer remove_block + add_block (trade-block’s eval state outputs torn re-evaluated), asks user whether proceed. Recover wrong arg. “Add head block 5 rows.” (model picks wrong arg name). “Sorry, use n rows.” Expected: model’s first modify_block returns staged creation turn error; model recovers calling remove_block followed add_block corrected args, flushed payload turn end carries exactly one add corrected arg. Manual checks documentation, gating — verify design holds real assistant loop recorded PR description.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/4-mutation-tools.html","id":"known-limitations-carried-into-later-phases","dir":"Articles > Design","previous_headings":"","what":"Known limitations carried into later phases","title":"Mutation tools","text":"Reads remain committed-. -turn read--write. model relies tool-call history staged. Phase 5’s prompt-context layer future list_pending_changes() tool can add explicit visibility empirical usage shows need. Stage-time mod validation shape-. Substantive validation delta (valid arg names, type compatibility) happens flush via core’s validator chain, stage time. typo’d arg name modify_block flushes rejects turn end. user sees standard Shiny notification; model hears via next turn’s user prompt (via Phase 5’s prompt-context layer lands). add → mod reject, collapse. Phase 3’s lenient -turn self-correction extend changing staged add’s args. model issue remove_block + add_block corrected args. Cost bounded (extra tokens -turn self-correction); benefit recipe cache second reactiveVal extension. empirical use shows model trouble recovery pattern, future phase can reintroduce collapse behind sidecar recipe cache. add_block / modify_block args JSON-formatted strings, native typed objects. ellmer’s type_object requires closed key list, escape hatch (.additional_properties = TRUE) deprecated supported Claude. JSON-string portable workaround. Trade-: model must format args JSON string rather emitting native object, tool body parses jsonlite::fromJSON. clean upgrade path — per-block-type tool generation backed registry type info — tracked #12. Stack subclass ctor args unreachable assistant. add_stack(blocks, name, id) modify_stack(id, blocks, name) expose base blockr.core::new_stack args, e.g. color attribute blockr.dock::dock_stack set modified assistant — falls back auto-promoter’s default apply time. architectural cause #12 applied stacks; tracked separately #13. Links face gap subclasses ever introduced (none today, urgency). Pinned upstream Remotes:. matching changes blockr.core blockr.dock released, DESCRIPTION pins via Remotes: PR branches: blockr.core#176 (delta-shape mod payload + update_link/update_stack generics) blockr.dock#133 (stacked #132, fixes two dock-side observer issues #131 #130 surfaced Phase 4 validation). Live end--end testing Claude confirmed pinned versions behave correctly: add_block / add_link / add_stack build pipelines, modify_block ctrl-able args updates board place without dock observer errors. Unpin release time. Stage-time link validator apply-time preprocess disagree dangling links (blockr.core). model removes block whose existing links still referenced elsewhere board, tries add new link, stage-time validate_board_update_xrefs rejects \"Expecting links refer known block IDs.\" — even though preprocess_board_update auto-clean orphans apply time. model explicitly remove_link dangling links adding new ones. Tracked blockr.core#177. Workaround assistant side mirror preprocess’s orphan-cleanup stage_block_rm; now prefer upstream fix behaviour stays single-source. External-control writes still go update(). design’s bet blockr.core#175. core resolves issue less-unified approach (e.g. ctrl writes still bypass update() consumers must route), Phase 4 need carve-modify_block route ctrl-eligible subset directly. Tracked. Atomic flush partial-success fallback. Inherited Phase 3 unchanged: racing user edit invalidates payload dispatch time discards whole turn’s mutations. snapshot / undo. Inherited Phase 3 / roadmap.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"goal","dir":"Articles > Design","previous_headings":"","what":"Goal","title":"Context, examples and polish","text":"Phase 5 closes initial roadmap giving model context needs use Phase 2 + Phase 4 tool surface well, giving user docs demos needed adopt package. new LLM-facing tools, new staging semantics — ships : turn--turn refreshed system prompt bundles static intro / conventions block, auto-generated tool catalogue, compact summary current board. small feedback channel tells model previous turn’s flushed payload rejected dispatch (can recover next turn without user retype error). top-level tutorial vignette, example app catalogue, pkgdown site organised around “use ” / “extend ” / “works”. README reflects full feature set rather Phase 1 ceiling. interesting architectural piece prompt-context layer: every earlier phase carved hole (“model sees board tool calls”, “flush rejection invisible model unless user types back”, “tool list hard-coded prompt”), Phase 5 fills hole. rest documentation work mechanical package surface settles.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"scope","dir":"Articles > Design","previous_headings":"","what":"Scope","title":"Context, examples and polish","text":": default_system_prompt(board, client, last_flush, ...) function builds four-section prompt. ’s default value new_assistant_extension’s system_prompt argument called extension server every refresh, result shipped via client$set_system_prompt(...). summarise_board() — token-budgeted text summary current board (blocks, links, stacks), formatted LLM consumption. Internally walks entity via new summarise_block / summarise_stack S3 generics block / stack authors can override per-entity line class (parallel Phase 2 describe_block / describe_stack pattern). format_tool_catalogue() — derives “tools” section client$get_tools() instead hand-curated text prompt body. Reflects whatever registered, including user extensions. last_flush_error reactiveVal populated Phase 3’s flush_pending dispatch-time validator rejection. Surfaces prompt one-line “previous changes rejected X” note, clears consumed. new query_data(code) read tool: evaluates model-supplied R environment built blockr.core::eval_env() every committed block’s result bound id, captures stdout + return value via capture.output, returns captured text. escape hatch questions static board summary can’t cover (unique values, group counts, ad-hoc filters, cross-block joins) without bloating prompt context per-block result previews. One showcase example app inst/examples/populated-board/ exercising populated board, mutation tools, new prompt context. vignettes/intro.Rmd — user-facing top-level tutorial. Lives next existing design vignettes package root pkgdown surfaces “Get started” article. updated README.Rmd reflecting full feature set. _pkgdown.yml organisation: “Get started” article, “Design notes” section grouping phase docs, reference index grouped surface (extension, tools, S3 generics). Tests default_system_prompt flush-feedback round-trip. (deferred later phases roadmap): New mutation tools. Phase 4 surface final initial roadmap. (One new read tool — query_data — lands phase; see scope .) list_pending_changes() tool. Phase 4 left follow-empirical use shows model confused committed-reads turn. Phase 5’s compact board summary covers read side friction; ’ll decide pending-list tool based actual usage rather speculatively. “pending changes” UI affordance (badge, diff preview). reactiveVals Phase 3 still observable; bolting UI cheap, user asked yet. Per-block-type custom tools (#12, #13). Snapshot / undo. Still gated blockr.session hooks exist. Multi-board / multi-session orchestration. Cross-session conversation persistence wiring. Phase 1 made round-trip-capable; wiring layer (e.g. blockr.session integration) unchanged . Inline tool-call approval UI. Phase 4’s mutation tools dispatch via staging layer turn end; “review apply” gate future affordance, Phase 5 deliverable.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"why-dynamic-context-at-all","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Why dynamic context at all","title":"Context, examples and polish","text":"Every prior phase landed finding: static system prompt combined -demand tool calls covers “board” question noticeable token latency cost. Concretely, two-turn pattern observed Phase 4 manual testing — User: “blocks ?” Model: → list_blocks() Model: “three blocks: data, head, plot.” …costs one round-trip per trivial question, list_blocks result re-fetched every turn model wants reason board shape. Folding compact board summary system prompt removes round-trip easy questions gives model coherent mental model entering every turn. expensive tools (describe_block, get_block_result) stay — drill-demand — shape board permanently context. Three observations Phase 4 push direction: model already discovers tool surface calling tool reading description. ’s wasteful can ship catalogue prompt directly. ellmer exposes registered tools via client$get_tools(), generating catalogue structural projection existing data — hand-maintained list drift. Flush-time rejections invisible model. Phase 3 shipped known limitation. mutation tools live, model can confidently report “added block” dispatch reject reason user can see Shiny toast model . Surfacing one-line “previous turn rejected” note next turn lets model recover without user retyping error. ID-change requests need system-prompt nudge. Phase 4’s architectural decision immutable committed ids correct, model way know without trying failing. short note intro block (“committed ids immutable; offer remove + add user asks rename”) cheaper failed tool call.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"the-system-prompt-is-a-function-with-a-string-shorthand","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"The system prompt is a function, with a string shorthand","title":"Context, examples and polish","text":"Phase 1’s system_prompt = \"<full prompt string>\" argument right shape static prompt, Phase 5 wants prompt reflect live board state every new turn. resolve extending argument’s type rather splitting : system_prompt argument now accepts function (called turn build prompt) string (used verbatim static prompt). default function default_system_prompt, ships four-section composition (intro + tool catalogue + board summary + optional flush-rejection note). semantics: Default (function): called every refresh (board, client, last_flush). function decides prompt looks like — package’s default_system_prompt four-section composition, caller can write function signature. String: wrapped internally function(...) the_string refresh path branch-free. user opting static string takes full control: auto-appended catalogue, auto-appended board summary, refresh activity. deal “give dynamic context, gain full prompt control”. user-extending--default pattern stays clean: signature (board, client, last_flush, ...). positional args reactives extension server holds; ... forward-compatibility headroom. default default_system_prompt accepts shape, recommendation user-supplied functions : accept ... adding arg call site future phase (e.g. pending -turn read--write ever lands; sibling-extension context assistant_tools axis ever lands — see #4) doesn’t break custom functions written today. state shape changes collapse: system_prompt function (custom default), state payload omits key entirely blockr.dock’s .call(ctor, payload) falls back default restore. ’s string, literal string stored. Phase 4 saved boards stored literal string still restore cleanly Phase 5’s wrapped-string path. Custom functions round-trip — fundamental limitation function-valued argument ser/des, since closure environments don’t serialise robustly. caller passing custom system_prompt function must re-pass every mount. documented Known limitations .","code":"new_assistant_extension(system_prompt = default_system_prompt, ...) my_prompt <- function(board, client, last_flush, ...) {   paste(     \"EXTRA: prefer modify_block over remove + add when possible.\",     \"\",     default_system_prompt(board, client, last_flush),     sep = \"\\n\"   ) }  new_assistant_extension(system_prompt = my_prompt) state_payload <- list(messages = messages)  if (is.character(system_prompt)) {   state_payload$system_prompt <- system_prompt }  list(state = state_payload)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"composition-cadence-drive-refresh-from-materialized-state","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Composition cadence: drive refresh from materialized state","title":"Context, examples and polish","text":"composed prompt track two things: live board (model sees current state) previous turn’s flush outcome (model sees rejection note). signals reactive values extension already access , refresh driven observation rather scheduled hook points: board$board post-validation post-apply value held blockr.core’s board reactiveValues. changes priority--Inf applier observer runs update(...) payload passed priority-Inf validator. reading reactively gives us free “update materialized” event: validation failures don’t change (refresh ), materialization timing wanted. single observer covers every refresh trigger care : last_input observer keeps reset_pending record_new_turns longer calls refresh_prompt — board observer covers last_input used cover, adds mid-turn case. last_turn observer keeps flush_pending record_new_turns; prompt refresh follows flush_pending’s reactiveVal writes, observer’s body. -flight stream caveat: ellmer::Chat$set_system_prompt mutates private$.turns R6 chat object — subsequent reads (next request tool-call loop, next user turn) see update. currently-streaming request body already sent can’t amended; whatever mid-flight finishes prompt started . Cancelling stream every UI edit strictly worse — UI edits unrelated current exchange burn turns. Ordering reactive writes inside last_turn observer: flush_pending writes last_flush_error (set clear) returning. board observer doesn’t fire next reactive flush current observer body finishes, refresh sees new value last_flush_error (new value board$board flush succeeded).","code":"observe({   board$board               # any successful materialization   last_flush_error()        # rejection-note signal   refresh_prompt() })"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"section-structure-of-the-default-prompt","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Section structure of the default prompt","title":"Context, examples and polish","text":"default_system_prompt assembles four sections fixed order. Order fixed token-cache hits Anthropic’s API equivalent behaviour others rely prefix stability — sections change infrequently come first. Intro / conventions (rarely changes). free-text introduction plus conventions don’t auto-generate (staging semantics, committed-inspection reads, id immutability, modify_block boundary). Inlined default function body; caller writing system_prompt function controls section entirely. Tool catalogue (changes tools registered removed — effectively never within session). One line per tool: <name>(<args>): <description>. Generated client$get_tools() walking arguments@properties ToolDef emitting compact signature. Description description field, trimmed de-newlined one line. Board summary (changes whenever board$board materializes change). current shape board: one-line header (N blocks, M links, K stacks), per-block / per-link / per-stack lines (see Board summary content ). Empty boards emit header plus explicit “(blocks yet)” note rather three empty tables. summary always reflects committed board (consistent Phase 4’s committed-read-tool surface). Delta note (one-shot, last_flush_error non-NULL). \"Note: previous turn's changes rejected: <reason>. board change.\". Cleared next successful -op flush. Sections joined blank-line separators. non-empty section one-line header (## Board:, ## Tools:, …) model can navigate prompt without parsing.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"board-summary-content-and-token-budget","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Board summary content and token budget","title":"Context, examples and polish","text":"summary structured surface model needs plan mutation without forcing call describe_block every block. per-entity projections delivered two new S3 generics block / stack authors can override per class: Per block — summarise_block(x, board, id). Default base block class returns one line: <id> (<class>): <arg=val, ...> [modifiable: <keys>]. args come initial_block_state(x); modifiable keys external_ctrl attribute (resolved way describe_block.block ). modifiable-keys field key extra Phase 4’s read tools: lets model pick modify_block vs remove + add without describe_block round-trip — exactly round-trip elimination Phase 5 exists deliver. included: block’s evaluated result (’s get_block_result’s job — large prompt context), display name + source package (registry metadata, looked model via list_available_blocks actually needs add block). Per link — flat row board_links(): <id>: <> -> <>$<input>. generic; links subclassable blockr.core’s current API. Per stack — summarise_stack(x). Default base stack class returns existing describe_stack-shape projection (name + comma-separated blocks); internal summarise_stacks helper prepends id. Stack classes add attributes (e.g. blockr.dag::dag_stack colour) override class denser one-line representation. summarise_block summarise_stack slot alongside existing describe_block / describe_stack (Phase 2) summarise_result (Phase 2). Three generics, three aspects block reaches model — describe_* full drill-projection used read tool, summarise_* compact prompt-context projection used dynamic system prompt, summarise_result result-value projection used get_block_result. Block authors can override subset. soft cap ~4000 characters total board section keeps prompt sub-1k-tokens moderately busy board (intro + tool catalogue bounded ~1500 chars count shape tools). board exceeds cap, truncate per-block arg values aggressively fall back header-summary (“Board 47 blocks; call list_blocks see .”) boards otherwise blow budget. Models larger context windows rarely hit cap; truncation safety belt, steady state. deliberately surface get_block_result previews prompt summary. result expensive thing format, volatile (changes every re-evaluation), likely irrelevant next user turn. model already get_block_result cases cares; preempting waste tokens every turn occasional value.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"tool-catalogue-auto-generation","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Tool catalogue auto-generation","title":"Context, examples and polish","text":"Phase 1 / Phase 2 default prompt hard-coded tool names. nine mutation tools (Phase 4) plus six read tools (Phase 2), plus possibility user-registered tools, hand-curated list long maintenance hazard. default_system_prompt derives client$get_tools(): format_tool_signature walks tool@arguments@properties (ellmer wraps arg list inside TypeObject whose @properties slot holds per-argument TypeBasic entries) produces name(arg1, arg2, ...), marking optional arguments (@required FALSE) ? required ones bare. client$get_tools() returns tools registration order, catalogue stable across runs — useful property prompt-cache hit rates providers key prefix bytes. consequence: model encounters tool catalogue, means tool registered last last_input refresh. Phase 5 ships late-registration mechanism, case hypothetical. future phase adds dynamic tool registration (e.g. per-block-type custom tools — #12), catalogue need refresh tool-set change .","code":"format_tool_catalogue <- function(client) {    tools <- client$get_tools()    if (!length(tools)) {     return(\"No tools registered.\")   }    lines <- chr_ply(tools, function(t) {     sig <- format_tool_signature(t)     desc <- gsub(\"\\\\s+\", \" \", t@description)     sprintf(\"- `%s`: %s\", sig, desc)   })    paste(lines, collapse = \"\\n\") }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"flush-rejection-feedback-as-a-one-shot-delta-note","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Flush-rejection feedback as a one-shot delta note","title":"Context, examples and polish","text":"Phase 4’s flush_pending already wraps update(...) dispatch tryCatch emits warning(...) rejection. Phase 5 extends optional last_flush_error reactiveVal outcome dispatch can surfaced model next turn. arg optional existing test callers (flush_pending(pending_update, update) tests/testthat/test-extension.R) keep working unchanged. last_flush_error reactiveVal sits extension server scope alongside pending_update. part state — like pending, ’s transient turn-local scratch, contractual surface. clearing -op branch important: without , rejection turn N survive past turn N+1 N+1 happened -mutation turn (e.g. user asks question rather issuing follow-edit), stale note show N+2’s prompt. invariant want “delta note always reflects recent flush attempt”; clearing -op preserves . default_system_prompt reads reactiveVal every refresh emits note non-NULL; full implementation lives sketch .","code":"flush_pending <- function(pending, update, last_flush_error = NULL) {    payload <- isolate(pending())    if (!has_any_changes(payload)) {     reset_pending(pending)     if (!is.null(last_flush_error)) last_flush_error(NULL)     return(invisible(FALSE))   }    tryCatch(     {       update(payload)       if (!is.null(last_flush_error)) last_flush_error(NULL)     },     error = function(e) {       if (!is.null(last_flush_error)) {         last_flush_error(conditionMessage(e))       }       warning(         \"flush_pending: dispatch rejected payload: \",         conditionMessage(e),         call. = FALSE       )     },     finally = reset_pending(pending)   )    invisible(TRUE) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"default-prompt-content","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Default-prompt content","title":"Context, examples and polish","text":"Phase 4 default_system_prompt body splits Phase 5: static prose — staging semantics, committed-inspection reads, id immutability, modify_block / remove + add boundary, “answer concisely” — inlined first section default_system_prompt’s output. per-tool listing drops entirely: auto-generated catalogue replaces . JSON-args--string note, already lives add_block / modify_block tool descriptions reaches model via catalogue.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"defensive-error-handling-around-the-system_prompt-call","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"Defensive error handling around the system_prompt call","title":"Context, examples and polish","text":"function bound system_prompt runs per refresh, every materialized board change. failure path needs exist package default (bug summarise_board format(<exotic-block-class>)) user-supplied functions (arbitrary code). wrap tryCatch inside refresh_prompt : surfaces failure user via blockr.core::notify(..., type = \"error\"), renders Shiny toast logs error level — path Phase 4’s mutation tools Phase 3’s flush rejection use, failure diagnostic footprint assistant-side error, skips set_system_prompt call failure (chat client keeps previous prompt — least-surprising fallback; matches Shiny’s observer error handling leaves prior reactive values place), substitute different prompt (e.g. default_system_prompt) — silently swapping prompt contents hide caller actually asked . first refresh fails (initial mount, previous prompt fall back ), chat client retains empty placeholder passed chat_ctor construction time. model operate without board context next refresh succeeds. package bug default fault, caller bug custom function fault — error notification points root cause either way.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"query_data-an-eval-escape-hatch-for-richer-inspection","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"query_data: an eval escape hatch for richer inspection","title":"Context, examples and polish","text":"get_block_result returns fixed summarise_result() projection single block — useful “look like” inadequate “many distinct values Species ”, “’s mean column X grouped Y”, “rows block block B match id”. static prompt can’t carry every projection, inflating per-block summary cover burn tokens every turn occasional value. remedy: query_data(code) read tool evaluates model-supplied R scoping env built blockr.core::eval_env() every committed block’s result bound id. Captures stdout (prints code ) auto-prints last expression’s value via capture.output. Returns captured text (truncated line cap runaway print(huge_df) doesn’t blow budget). Behavioural shape mirrors one-shot R REPL evaluation: Bindings: every block b successful result bound b env. Blocks errored evaluation time omitted, tool result lists one-line preface model knows wasn’t reachable. Parent env: eval_env()’s default chain — either default_eval_parent() (blockr-default packages attached) baseenv(), depending attach_default_packages blockr_option. Matches block’s expression evaluates, model’s mental model “’s scope” lines mutate_block (similar) see. Output: capture.output({ val <- eval(parsed, envir = env); print(val) }). Mid-script print calls land capture; last expression’s value auto-prints (REPL semantics). Errors parse eval time route with_tool_errors standard query_data failed: <reason> envelope. Truncation: hard cap ~200 lines captured output. cap, tool result first N lines + (output truncated; %d lines hidden) footer. Security trust: lets model execute arbitrary R app’s process. sounds alarming notice mutate-block class attack surface already reachable — add_block(\"mutate_block\", args = '{\"expr\": \"system(...)\"}') gets code execution via staging path. query_data doesn’t expand attack surface, just makes capability explicit ergonomic read-inspection. Anyone deploying assistant high-trust context (.e. real user data, real R session) already needs vet model provider data sources prompt-injection risk; adding query_data doesn’t change calculus. tool : sandboxed (callr subprocess, allowlist), time-limited (timeout — long-running queries block chat module), memory-bounded (large intermediate values stay eval env GC). hardening passes future phase can layer without changing tool’s surface.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"what-default_system_prompt-is-not","dir":"Articles > Design","previous_headings":"Architectural decisions","what":"What default_system_prompt is not","title":"Context, examples and polish","text":"deliberate non-features: -shot examples. Tempting green-field assistant, expensive tokens prone -fitting model example shape. tool descriptions board summary enough; user feedback shows model misuses tool, improve tool’s description rather ship -shot exemplar. interception -flight stream. Mid-turn UI edits refresh prompt, request currently wire finishes original prompt. model’s next request tool-call loop sees update. conversation summarisation / compression. ellmer handles context-window management provider boundary. re-summarise old turns; manipulate system prompt. diff vs. last turn’s summary. “Block X changed since last looked” useful introduces stateful comparison bookkeeping. flush-rejection note covers “previous-turn-mattered” case; diffing waits demand.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rsystem-prompt-r-extended","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/system-prompt.R (extended)","title":"Context, examples and polish","text":"","code":"#' Default assistant system prompt #' #' Builds the four-section system prompt the assistant ships by #' default: an intro / conventions block, an auto-generated tool #' catalogue from `client$get_tools()`, a compact board summary, #' and (when applicable) a one-line note carrying the previous #' turn's flush rejection. #' #' Each argument is optional; the corresponding section is omitted #' when its input is `NULL`, so `default_system_prompt()` at the #' REPL returns just the intro block — useful for inspecting what #' the default looks like without mounting a board. #' #' This is the default value of `new_assistant_extension`'s #' `system_prompt` argument. Custom functions passed in its place #' receive `(board, client, last_flush, ...)`; the `...` is #' forward-compatibility headroom for future phases adding inputs #' (accept `...` in custom functions so the call site can grow #' without breaking you). #' #' @param board Reactive containing the live board, as supplied to #'   the extension server. `NULL` omits the board section. #' @param client An `ellmer::Chat`. `NULL` omits the tool catalogue. #' @param last_flush Reactive holding the previous turn's flush #'   rejection message (character) or `NULL`. `NULL` (or a `NULL` #'   value) omits the delta note. #' @param ... Forward-compatibility slot for future inputs. #' #' @return A character scalar. #' #' @export default_system_prompt <- function(board = NULL, client = NULL,                                   last_flush = NULL, ...) {    intro <- paste(     \"You are an assistant embedded next to a blockr data analysis\",     \"board. The Tools section below lists what you can call; the\",     \"Board section is the current shape of the board.\",     \"\",     \"Inspection tools always read the committed board, not your\",     \"staged changes. Mutation tools *stage* a change; nothing\",     \"applies mid-turn. All staged calls from your turn flush as one\",     \"atomic update when your turn ends. Your own tool-call history\",     \"is the record of what is pending.\",     \"\",     \"Block, link and stack ids are immutable once committed. If the\",     \"user asks to rename one, explain you can offer remove + add\",     \"with a new id, but that tears down the block server and\",     \"re-evaluates downstream blocks -- ask before proceeding. For\",     \"a still-staged entity, use remove + add to change the id.\",     \"\",     \"modify_block can only change keys reported as modifiable in\",     \"the Board section above (and block_name, always). For other\",     \"changes use remove_block + add_block.\",     \"\",     \"Answer concisely.\",     sep = \"\\n\"   )    sections <- intro    if (!is.null(client)) {     sections <- c(       sections, \"\", \"## Tools\", format_tool_catalogue(client)     )   }    if (!is.null(board)) {     sections <- c(       sections, \"\", \"## Board\", summarise_board(board)     )   }    err <- if (!is.null(last_flush)) isolate(last_flush()) else NULL    if (!is.null(err)) {     sections <- c(       sections,       \"\",       sprintf(         \"Note: your previous turn's changes were rejected: %s. %s\",         err,         \"The board did not change. Re-issue corrected calls.\"       )     )   }    paste(sections, collapse = \"\\n\") }  format_tool_catalogue <- function(client) {    tools <- client$get_tools()    if (!length(tools)) {     return(\"(none)\")   }    lines <- chr_ply(tools, function(t) {     sprintf(       \"- `%s`: %s\",       format_tool_signature(t),       gsub(\"\\\\s+\", \" \", t@description)     )   })    paste(lines, collapse = \"\\n\") }  format_tool_signature <- function(tool) {    args <- tool@arguments@properties    if (!length(args)) {     return(sprintf(\"%s()\", tool@name))   }    parts <- chr_ply(names(args), function(nm) {     if (isTRUE(args[[nm]]@required)) nm else paste0(nm, \"?\")   })    sprintf(\"%s(%s)\", tool@name, paste(parts, collapse = \", \")) }  summarise_board <- function(board, max_chars = 4000L) {    b <- isolate(board$board)   blks <- board_blocks(b)   lnks <- board_links(b)   stks <- board_stacks(b)    # board_links() returns a vctrs record (links class); length()   # gives row count, nrow() returns NULL. Same shape as the value   # describe_block.block subsets with `links[links$to == id]`.   header <- sprintf(     \"%d block(s), %d link(s), %d stack(s).\",     length(blks), length(lnks), length(stks)   )    if (!length(blks) && !length(lnks) && !length(stks)) {     return(paste(header, \"(empty board -- no blocks yet)\"))   }    body <- c(     header,     \"\",     summarise_blocks(blks),     summarise_links(lnks),     summarise_stacks(stks)   )    out <- paste(body, collapse = \"\\n\")    if (nchar(out) > max_chars) {     return(       paste(         header,         \"(too many blocks to inline; call list_blocks for the full\",         \"list)\"       )     )   }    out }  summarise_blocks <- function(blks) {    if (!length(blks)) return(character())    c(     \"## Blocks\",     chr_ply(names(blks), function(id) {       summarise_block(blks[[id]], board = NULL, id = id)     })   ) }  summarise_links <- function(lnks) {    if (!length(lnks)) return(character())    df <- as.data.frame(lnks)   c(     \"## Links\",     chr_ply(       seq_len(nrow(df)),       function(i) sprintf(         \"- %s: %s -> %s$%s\",         df$id[[i]], df$from[[i]], df$to[[i]], df$input[[i]]       )     )   ) }  summarise_stacks <- function(stks) {    if (!length(stks)) return(character())    c(     \"## Stacks\",     chr_ply(names(stks), function(id) {       sprintf(\"- %s %s\", id, summarise_stack(stks[[id]]))     })   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rsummarise-block-r-new","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/summarise-block.R (new)","title":"Context, examples and polish","text":"","code":"#' Summarise a block for the LLM prompt context #' #' Generic backing the per-block lines in the dynamic system #' prompt's board summary. The default method `summarise_block.block` #' produces a compact one-line summary (id, class, current arg #' values, modifiable keys). Block authors override their class #' when the default is too generic or too verbose for prompt #' context. #' #' Parallel to [describe_block()] (which produces the *full* #' multi-line description used by the `describe_block` tool). Use #' `summarise_block` when token density matters; use #' `describe_block` when the user has explicitly asked for detail. #' #' @param x A `block`. #' @param board The current board snapshot, for resolving #'   cross-references. May be `NULL` — currently unused by the #'   default method, reserved for overrides that want to surface #'   relationships. #' @param id The id under which the block lives on the board. #' @param ... For future use. #' #' @return A single-line character scalar (preferred). A multi-line #'   vector is accepted but inflates the prompt budget. #' #' @export summarise_block <- function(x, board, id, ...) UseMethod(\"summarise_block\")  #' @rdname summarise_block #' @export summarise_block.block <- function(x, board, id, ...) {    args <- initial_block_state(x)   ctrl <- attr(x, \"external_ctrl\")    args_str <- if (length(args)) {      paste(       chr_ply(names(args), function(nm) {         sprintf(\"%s=%s\", nm, format(args[[nm]]))       }),       collapse = \", \"     )    } else {     \"no args\"   }    ctrl_str <- if (isTRUE(ctrl)) {     \"all args + block_name\"   } else if (isFALSE(ctrl) || !length(ctrl)) {     \"block_name only\"   } else {     paste(c(ctrl, \"block_name\"), collapse = \", \")   }    sprintf(     \"- %s (%s): %s [modifiable: %s]\",     id, class(x)[[1L]], args_str, ctrl_str   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rsummarise-stack-r-new","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/summarise-stack.R (new)","title":"Context, examples and polish","text":"","code":"#' Summarise a stack for the LLM prompt context #' #' Generic backing the per-stack lines in the dynamic system #' prompt's board summary. The default method `summarise_stack.stack` #' produces a compact name + member-blocks line. Stack classes that #' carry extra attributes (e.g. `blockr.dag::dag_stack` with a #' colour) override their class to surface those attributes. #' #' Parallel to [describe_stack()] (full description, used by the #' `list_stacks` tool's description column). Use `summarise_stack` #' when token density matters. #' #' @param x A `stack`. #' @param ... For future use. #' #' @return A single-line character scalar (preferred). The caller #'   in `summarise_board()` prepends the stack's id. #' #' @export summarise_stack <- function(x, ...) UseMethod(\"summarise_stack\")  #' @rdname summarise_stack #' @export summarise_stack.stack <- function(x, ...) {    blocks <- stack_blocks(x)   blocks_str <- if (length(blocks)) {     paste(blocks, collapse = \", \")   } else {     \"<empty>\"   }    sprintf(     \"'%s' (blocks: %s)\",     coal(stack_name(x), \"<unnamed>\"),     blocks_str   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rtool-query-data-r-new","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/tool-query-data.R (new)","title":"Context, examples and polish","text":"register_read_tools gains one line registering tool alongside Phase 2 read surface.","code":"tool_query_data <- function(board, update, session) {    ellmer::tool(     function(code) {       with_tool_errors(\"query_data\", {          blks <- isolate(board$blocks)          data <- list()         skipped <- character()          for (id in names(blks)) {            res <- tryCatch(             isolate(blks[[id]]$server$result()),             error = function(e) e           )            if (inherits(res, \"error\")) {             skipped <- c(skipped, id)           } else {             data[[id]] <- res           }         }          env <- eval_env(data)         parsed <- parse(text = code)          output <- capture.output({           val <- NULL           for (e in parsed) val <- eval(e, envir = env)           if (!is.null(val)) print(val)         })          if (length(output) > 200L) {           hidden <- length(output) - 200L           output <- c(             output[seq_len(200L)],             sprintf(\"(output truncated; %d lines hidden)\", hidden)           )         }          if (length(skipped)) {           output <- c(             sprintf(               \"(skipped blocks with errors: %s)\",               paste(skipped, collapse = \", \")             ),             \"\",             output           )         }          paste(output, collapse = \"\\n\")       })     },     name        = \"query_data\",     description = paste(       \"Evaluate R code against the board's block results. Every\",       \"committed block's evaluated result is bound in scope by its\",       \"block id (e.g. for a block with id `data` write `head(data)`).\",       \"Returns captured stdout plus the auto-printed value of the\",       \"last expression -- the same shape an R REPL would produce.\",       \"Use this for questions the Board section doesn't carry: unique\",       \"values, group counts, ad-hoc filters, joins across blocks.\",       \"Read-only; the board is not modified.\"     ),     arguments = list(       code = ellmer::type_string(         paste(           \"R code to evaluate. Multiple statements allowed; the last\",           \"expression's value is auto-printed.\"         )       )     )   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rextension-r-diff","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/extension.R (diff)","title":"Context, examples and polish","text":"Four diffs vs. Phase 4: Constructor’s system_prompt default flips NULL exported default_system_prompt function. Strings continue work — ’re wrapped server start function returns string. rename, new arg. New last_flush_error reactiveVal alongside pending_update. flush_pending gains third (optional) argument; existing callers passing two args keep working. New observe({ board$board; last_flush_error(); refresh_prompt() }) block drives refresh materialized state changes, covering -turn UI edits mid-turn UI edits (latter picked next request tool-call loop). explicit initial refresh_prompt() tool registration sets prompt observer settles first time — deterministic startup state. last_input observer drops prompt-refresh call (board observer covers ) keeps reset_pending record_new_turns. state shape now conditionally includes system_prompt: caller passed literal string. Function-valued system_prompt (custom function default) omitted state .call(ctor, payload) restore falls back constructor’s default. Phase 4 saved boards carry literal string state$system_prompt (Phase 4 always serialised resolved default text); still restore cleanly Phase 5’s wrapped-string path.","code":"new_assistant_extension <- function(system_prompt = default_system_prompt,                                     messages = NULL,                                     ...) {    new_dock_extension(     server = asst_ext_srv(system_prompt, messages),     ui     = asst_ext_ui,     name   = \"Assistant\",     class  = \"assistant_extension\",     options = new_board_options(new_llm_model_option()),     ...   ) }  asst_ext_srv <- function(system_prompt, messages) {    function(id, board, update, ...) {      moduleServer(       id,       function(input, output, session) {          chat_ctor <- isolate(           get_board_option_value(\"llm_model\", session)         )          # Normalise: a string becomes a function returning that         # string, so refresh_prompt() has no branching. force()         # captures the string by value before the closure forms.         compose <- if (is.function(system_prompt)) {           system_prompt         } else {           force(system_prompt)           function(...) system_prompt         }          pending_update   <- reactiveVal(empty_pending())         last_flush_error <- reactiveVal(NULL)          # Ctor needs an initial system prompt; pass a placeholder         # and overwrite it after tools are registered so the         # catalogue covers the full set on first composition.         client <- chat_ctor(system_prompt = \"\")          register_read_tools(client, board, update, session)         register_mutation_tools(           client, board, pending_update, session         )          refresh_prompt <- function() {            prompt <- tryCatch(             compose(board, client, last_flush_error),             error = function(e) {               notify(                 paste(                   \"Assistant prompt update failed:\",                   conditionMessage(e)                 ),                 type = \"error\"               )               NULL             }           )            if (!is.null(prompt)) {             client$set_system_prompt(prompt)           }         }          refresh_prompt()                                          # initial          if (length(messages)) {           client$set_turns(             lapply(messages, ellmer::contents_replay)           )         }          mod <- shinychat::chat_mod_server(\"chat\", client)          messages <- reactiveVal(coal(messages, list()))          record_new_turns <- function() {           # unchanged from Phase 3         }          # Single observer for prompt refresh; fires on any materialized         # board change or any change to the flush-rejection signal.         observe({           board$board           last_flush_error()           refresh_prompt()         })          observeEvent(           mod$last_input(),           {             reset_pending(pending_update)             record_new_turns()           },           ignoreNULL = TRUE         )          observeEvent(           mod$last_turn(),           {             flush_pending(pending_update, update, last_flush_error)             record_new_turns()           },           ignoreNULL = TRUE         )          output$tokens <- renderUI(           format_token_telemetry(mod$last_turn())         )          state_payload <- list(messages = messages)         if (is.character(system_prompt)) {           state_payload$system_prompt <- system_prompt         }          list(state = state_payload)       }     )   } }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"rstaging-r-diff","dir":"Articles > Design","previous_headings":"Implementation sketch","what":"R/staging.R (diff)","title":"Context, examples and polish","text":"Two changes vs. Phase 4’s flush_pending: Optional last_flush_error arg. Existing tests calling flush_pending(pending_update, update) keep working — feedback path -op arg NULL. reactiveVal write symmetric across three branches (-op / success / rejection). -op branch clears prior error flush-rejected turn followed -mutation turn doesn’t leave stale note visible turn +2. invariant default_system_prompt relies “delta note always reflects recent flush attempt”. Phase 3’s atomicity contract otherwise unchanged: whole staged payload either applies discarded; partial apply.","code":"flush_pending <- function(pending, update, last_flush_error = NULL) {    payload <- isolate(pending())    if (!has_any_changes(payload)) {     reset_pending(pending)     if (!is.null(last_flush_error)) last_flush_error(NULL)     return(invisible(FALSE))   }    tryCatch(     {       update(payload)       if (!is.null(last_flush_error)) last_flush_error(NULL)     },     error = function(e) {       if (!is.null(last_flush_error)) {         last_flush_error(conditionMessage(e))       }       warning(         \"flush_pending: dispatch rejected payload: \",         conditionMessage(e),         call. = FALSE       )     },     finally = reset_pending(pending)   )    invisible(TRUE) }"},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"top-level-tutorial-vignette-vignettesintro-rmd","dir":"Articles > Design","previous_headings":"Documentation deliverables","what":"Top-level tutorial vignette — vignettes/intro.Rmd","title":"Context, examples and polish","text":"Lives package root (vignettes/design/) browseVignettes(\"blockr.assistant\") pkgdown’s “Get started” slot pick . design-doc vignettes stay vignettes/design/ articles, accessed via pkgdown navbar. Outline (one Rmd, ~250 lines): . Two sentences plus screenshot populated board assistant panel. Mount board. Copy-paste runnable example: dock_board three blocks, assistant extension, served. Talk . worked dialogue showing model answering “blocks board?” (now answered prompt summary, tool call), “describe head block” (one tool call), “add scatter plot sepal length vs sepal width” (three staged mutations, one flush). Configure model. point different provider via blockr_option(\"chat_function\", …). Pointer ellmer provider matrix. Customise prompt. Pass string full static control, function full dynamic control (wrapping default_system_prompt() usual extension pattern). Extend prompt read tools. S3 generic family exposed package: describe_block / describe_stack (full descriptions, used read tools), summarise_block / summarise_stack (compact prompt-context lines), summarise_result (block result formatting). One short example bespoke block class overriding summarise_block describe_block demonstrate density / detail split. go next. Pointers design vignettes contributors, issue tracker feature requests. Code chunks evaluated (eval = FALSE) — package can’t talk live model CI without key. Screenshots ship man/figures/.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"example-apps-instexamples","dir":"Articles > Design","previous_headings":"Documentation deliverables","what":"Example apps — inst/examples/","title":"Context, examples and polish","text":"Two demos, named show rather phase introduced : empty-board/ — empty board, assistant builds scratch. Smoke-tests mutation surface end--end real LLM. populated-board/ — four-block pipeline (data → filter → head / plot) two stacks, wired assistant mounts. Gives model something navigate (just ). Comments top list manual scenarios (“ask ’s board without calling tool”, “rename block observe immutability nudge”, “ask ‘many unique species data?’ exercise query_data”). Phase 5 consolidates per-phase example directories (01-shell/, 02-read-tools/) populated-board/ renames 04-mutation-tools/ → empty-board/. single phase-5 showcase becomes populated-board/.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"readme-readme-rmd","dir":"Articles > Design","previous_headings":"Documentation deliverables","what":"README — README.Rmd","title":"Context, examples and polish","text":"surgical edit, rewrite. existing README mostly fine — badges, install, runnable example data + head + plot, screenshot still apply. stale piece “Roadmap” section pins lifecycle Phase 1 says “assistant … tools inspect manipulate board yet”. Replace one-paragraph status pointer: assistant feature-complete initial roadmap. See roadmap article staged plan per-phase design notes. Lifecycle badge stays experimental (still pre-1.0). example screenshot may refreshed Phase 5 example app, ’s man/figures/ swap, README change.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"pkgdown-_pkgdown-yml","dir":"Articles > Design","previous_headings":"Documentation deliverables","what":"pkgdown — _pkgdown.yml","title":"Context, examples and polish","text":"Two changes: Articles navbar: “Get started” entry pointing vignettes/intro.Rmd, “Design notes” section surfaces six design vignettes roadmap order. Reference index: grouped “Extension” (new_assistant_extension, default_system_prompt), “S3 generics” (describe_block, describe_stack, summarise_block, summarise_stack, summarise_result), unlabelled miscellany anything fit (currently empty — package surface intentionally small). pkgdown site already deployed via existing CI workflow; infra changes.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"files-added-changed","dir":"Articles > Design","previous_headings":"","what":"Files added / changed","title":"Context, examples and polish","text":"R/system-prompt.R — rewrite default_system_prompt zero-arg static-string function four-section builder documented (signature (board = NULL, client = NULL, last_flush = NULL, ...)). Add internal helpers: format_tool_catalogue, format_tool_signature, summarise_board, summarise_blocks, summarise_links, summarise_stacks. intro / conventions text inlined function body (separate helper). R/summarise-block.R (new) — summarise_block S3 generic + default summarise_block.block method emitting compact per-block line. R/summarise-stack.R (new) — summarise_stack S3 generic + default summarise_stack.stack method emitting compact per-stack line. R/tool-query-data.R (new) — tool_query_data factory; read-eval-print escape hatch documented . R/tools-read.R — register tool_query_data register_read_tools alongside Phase 2 read tools. system_prompt NULL default_system_prompt, accept function string, wrap strings function(...) the_string server start, add last_flush_error reactiveVal, add refresh_prompt() helper last_input call site, switch state conditionally include system_prompt literal string. R/staging.R — extend flush_pending optional last_flush_error reactiveVal write (success -op clear, rejection populates). inst/examples/populated-board/app.R (new) — showcase demo. vignettes/intro.Rmd (new) — user-facing tutorial. README.Rmd / README.md — rewrite per section . _pkgdown.yml — reference index grouping; “Get started” article. tests/testthat/test-system-prompt.R (new) — default_system_prompt formatter coverage: default_system_prompt() (args) returns intro block; client adds catalogue; board adds summary; populated last_flush reactiveVal adds delta note. format_tool_signature shape. summarise_board empty / small / oversized boards. tests/testthat/test-extension.R — update existing default_system_prompt -arg test assert intro-return called bare. Replace existing asst_ext_srv(system_prompt = NULL, ...) callers explicit function-valued args (default_system_prompt stub), since new server longer coalesces NULL default. Add tests string-arg path (state carries string verbatim, refresh sets string turn), function-arg path (state omits system_prompt restore falls back default), observe({ board$board; last_flush_error() }) refresh observer wiring, flush-feedback round-trip (rejected dispatch populates last_flush_error, refresh surfaces note, successful follow-flush clears ). tests/testthat/test-staging.R — extend flush_pending tests optional last_flush_error reactiveVal behaviour (success clears, rejection populates, -op also clears stale error can’t survive quiet turn). vignettes/design/0-roadmap.Rmd — fix stale <n>-<topic>.qmd references (10 occurrences) .Rmd reflect PR #7’s conversion, add Phase 5 entry vignette index table. Pure docs drift; code impact. NEWS.md (new absent) — system_prompt argument now accepts function (new default default_system_prompt), flush-rejection feedback, pkgdown article reorganisation. DESCRIPTION — bump version 0.1.0 reflecting initial roadmap completion. new imports — client$get_tools(), client$set_system_prompt(), eval_env() existing ellmer / blockr.core surface. New exports: rewritten default_system_prompt (name Phase 4, signature change ), summarise_block, summarise_stack. helpers internal.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"automated-ci-devtoolscheck","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Automated (CI / devtools::check())","title":"Context, examples and polish","text":"default_system_prompt() called arguments returns just intro block (## Tools, ## Board, delta note); client adds catalogue header tool lines; board adds board header per-entity lines; populated last_flush reactiveVal adds delta note. format_tool_catalogue() chat full Phase 2 + Phase 4 tool set emits one line per tool registration order, optional args marked ? required args bare. empty board → header + “(empty board – blocks yet)”. small populated board → header + per-block / per-link / per-stack lines. pathologically large board (1000 synthetic blocks) → header + “(many blocks inline; call list_blocks…)” truncation branch. summarise_block dispatches per class: register fake S3 method summarise_block.fake_class returning \"OVERRIDDEN\", build board containing block class, assert summarise_board() output block \"OVERRIDDEN\" (default-class blocks unchanged). summarise_stack dispatches per class: shape block test, fake stack class. query_data happy path: board dataset_block named d serving iris, calling tool code = \"nrow(d)\" returns \"[1] 150\". code = \"length(unique(d$Species))\" returns \"[1] 3\". multiple statements (\"x <- table(d$Species); x\") returns auto-printed table. query_data error paths: parse error code returns \"query_data failed: <reason>\" string (via with_tool_errors); runtime error eval returns envelope; board failing block surfaces \"(skipped blocks errors: …)\" preface blocks still resolve scope. query_data truncation: code argument auto-prints 10000-row data frame produces output capped 200 lines plus (output truncated; N lines hidden) footer. Refresh observer fires board materialization: stage flush non-empty payload via shiny::testServer(), assert client$get_system_prompt() reflects new board flush (refreshed board$board observer, last_input hook). Refresh observer fires UI-driven board change: directly mutate board$board (simulating UI edit), assert client$get_system_prompt() reflects new state without last_input / last_turn activity . Failing system_prompt function: pass system_prompt = function(...) stop(\"boom\"). Trigger refresh. Assert () client$get_system_prompt() unchanged previous value, (b) notification type \"error\" emitted (mock blockr.core::notify, capture call), (c) chat module still alive — observer crash. Stage payload fails mocked validator. Trigger last_turn. Assert last_flush_error() holds rejection message client$get_system_prompt() contains delta note (refresh fires last_flush_error observation). Trigger last_turn -op turn. Assert last_flush_error() NULL composed prompt longer carries note. String-arg path: passing system_prompt = \"<custom>\" new_assistant_extension results client$get_system_prompt() returning exactly \"<custom>\" initial refresh, state payload carries system_prompt = \"<custom>\" verbatim. Function-arg path: passing system_prompt = function(...) \"X\" results client$get_system_prompt() returning \"X\", state payload omits system_prompt key entirely. state round-trip: serialising extension built custom string, restoring , reading client$get_system_prompt() returns string. Built default function, restored extension uses default function (stale snapshot). Phase 1-4 acceptance criteria continue pass. devtools::check() passes 0/0/0. lintr::lint_package() passes 0 lints.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"manual-demo-app-live-llm","dir":"Articles > Design","previous_headings":"Acceptance criteria","what":"Manual (demo app, live LLM)","title":"Context, examples and polish","text":"Run Phase 5 example app real provider verify: Trivial question without tool call. “many blocks board?” Expected: model answers prompt summary without invoking list_blocks. Inspect ellmer’s turn record confirm tool call fired. Drill-tool call. “Show current value n_rows head block.” Expected: one describe_block call, accurate answer. query_data drill-. “many unique values Species data block carry, ?” Expected: one query_data call (e.g. unique(data$Species)), answer cites three setosa/versicolor/virginica values captured output. Confirms eval escape hatch used questions static summary can’t answer. Pipeline build. “Add filter Sepal.Width > 3 head block pipe result new scatter plot.” Expected: model stages appropriate blocks links one turn; flush succeeds; new blocks appear board. Rename request. “Rename head top_rows.” Expected: model declines -place rename, explains remove + add path eval-state caveat, asks user. (Triggered id-immutability paragraph default prompt’s intro; tool call required.) Recover flush rejection. Set board staged link create cycle (manual setup via example app’s comments). Ask assistant add cycle-creating link. Expected: model reports staging success per tool call, flush rejects; next user prompt (“try differently”), model sees delta note prompt proposes non-cyclic alternative without user paste error. Mid-turn UI edit. assistant busy multi-tool-call turn (e.g. “build small pipeline”), edit block’s args via UI (change dataset selection dataset block, say). Expected: currently-streaming LLM request finishes original prompt, next request turn’s tool-call loop sees updated board summary. Inspect asking model follow-turn whether noticed edit; reference new args without fresh tool call. Manual checks documentation, gating — verify prompt-context layer holds real assistant loop recorded PR description.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/design/5-polish.html","id":"known-limitations-carried-into-later-phases","dir":"Articles > Design","previous_headings":"","what":"Known limitations carried into later phases","title":"Context, examples and polish","text":"Mid-turn UI edits can’t reach -flight LLM request. Prompt refresh driven observer board$board, successfully materialized board change — including UI edit assistant working — triggers fresh client$set_system_prompt(...). Subsequent requests tool-call loop pick update . currently-streaming request body, however, already wire finishes prompt started . Cancelling stream every UI edit strictly worse (unrelated edit kills model’s -flight reply), accept gap. diff last turn’s summary. “Block X’s n_rows changed since last looked” help model spot user edits, introduces stateful comparison bookkeeping. flush-rejection note delta ship; richer diffs wait demand. Tool catalogue bag--tools, categorised. catalogue lists every registered tool registration order. current 16-tool surface readable; per-block-type custom tools (#12) ever land, grouping namespace might matter. query_data runs unsandboxed -process. Model-supplied code reaches default_eval_parent() (baseenv()), scope mutate_block expression. timeout, memory cap, system() allowlist. architectural decision section argues acceptable attack surface exists via add_block(\"mutate_block\", ...); future hardening phase can layer subprocess isolation (callr), expression allowlists, time/memory limits without changing tool’s surface. Static-string override loses dynamic context. Passing system_prompt = \"<string>\" opts refresh entirely. caller wants default composition plus small extra line writes function calls default_system_prompt(...) concatenates — extension “default + append” sugar. Adding sugar cheap real use shows matters. prompt-cache awareness. default_system_prompt assembles sections stable order providers cache prefix bytes (e.g. Anthropic’s prompt caching) get decent hit rates, don’t surface explicit cache breakpoint hints. ellmer may grow API; wire /appears. list_pending_changes() deferred. Phase 4 left door open explicit “show ’s staged” tool empirical use showed model getting confused. Phase 5’s compact board summary covers read side friction; ’ll observe add tool real users hit . conversation persistence wiring. Inherited Phase 1. state payload (system_prompt string, messages always) round-trips via blockr.dock’s ser/des, cross-session save/restore (e.g. via blockr.session) still application-level integration. snapshot / undo. Inherited roadmap. inline tool-call approval UI. Inherited Phase 4. Mutation tools dispatch via staging turn end; “review apply” gate future affordance. Custom-function system_prompt round-trip via ser/des. Function values deliberately omitted state payload, board saved custom function reloads using default function. Functions don’t serialise robustly (closure environments carry application-specific bindings), alternative — silently freezing rendered string save time — surprising reverting default. Callers need custom function re-supply mount time session.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"mount-it-on-a-board","dir":"Articles","previous_headings":"","what":"Mount it on a board","title":"Get started with blockr.assistant","text":"’s whole setup. assistant takes chat client board’s llm_model option (resolved blockr.core’s default_chat unless overridden), registers read mutation tools, starts chat module.","code":"library(blockr.core) library(blockr.dock) library(blockr.assistant)  board <- new_dock_board(   blocks = c(     data = new_dataset_block(\"iris\"),     head = new_head_block(n = 10L),     plot = new_scatter_block(x = \"Sepal.Length\", y = \"Sepal.Width\")   ),   links = c(     new_link(\"data\", \"head\", \"data\"),     new_link(\"head\", \"plot\", \"data\")   ),   extensions = list(assistant = new_assistant_extension()),   layout = list(     list(\"data\", \"head\", \"plot\"),     \"assistant\"   ) )  serve(board)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"talk-to-it","dir":"Articles","previous_headings":"","what":"Talk to it","title":"Get started with blockr.assistant","text":"system prompt model receives new turn contains three sections: fixed intro / conventions block, auto-generated tool catalogue, compact board summary. Trivial questions land without tool round-trip: : many blocks board? Assistant: three blocks: data (dataset_block), head (head_block), plot (scatter_block). Richer questions specific block trigger one drill-call: : n_rows head block? Assistant: $$calls `describe_block(id = \"head\")`$$ n_rows currently 10. Questions block’s data go query_data tool, evaluates R environment every committed block result bound id: : unique species data block? Assistant: $$calls `query_data(code = \"unique(data$Species)\")`$$ Three: setosa, versicolor, virginica. Mutations stage across turn flush atomically assistant finishes: : Add filter Sepal.Width > 3 head plot. Assistant: $$stages `add_block(...)`, `add_link(...)`, `add_link(...)`, flushes $$ Added.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"configure-the-model","dir":"Articles","previous_headings":"","what":"Configure the model","title":"Get started with blockr.assistant","text":"chat backend comes global option: ellmer::chat_*() constructor follows standard (system_prompt, params) shape . See ellmer’s provider matrix options credential handling.","code":"library(ellmer) blockr.core::blockr_option(   \"chat_function\",   function(system_prompt = NULL, params = NULL) {     chat_anthropic(model = \"claude-sonnet-4-5\", system_prompt = system_prompt)   } )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"customise-the-prompt","dir":"Articles","previous_headings":"","what":"Customise the prompt","title":"Get started with blockr.assistant","text":"new_assistant_extension(system_prompt = ...) accepts either: string – used verbatim static prompt, refresh, auto-appended board context. take full control: function – called refresh (board, client, ...), returning prompt string. default default_system_prompt composes four-section default. extend , wrap call . Custom functions accept ... forward-compatibility future phases adding inputs. can call default_system_prompt() arguments REPL inspect just intro block.","code":"new_assistant_extension(   system_prompt = \"You are a terse SQL coach. Refuse non-SQL.\" ) my_prompt <- function(board, client, ...) {   paste(     \"ADDITIONAL RULE: prefer modify_block over remove + add\",     \"when possible.\",     \"\",     default_system_prompt(board, client, ...),     sep = \"\\n\"   ) }  new_assistant_extension(system_prompt = my_prompt)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"teach-it-your-conventions-with-skills","dir":"Articles","previous_headings":"","what":"Teach it your conventions with skills","title":"Get started with blockr.assistant","text":"block type’s guidance package author’s view configure . skill deployment’s view – particular flexible block , , preferably used certain way. Skills markdown files disk, domain expert write R can contribute one, cost nothing per turn model reads . skill directory whose name skill name; SKILL.md entry point sibling files optional bundled resources model pulls needs . SKILL.md YAML frontmatter plus markdown body: name (must match directory hold nothing lowercase letters, digits hyphens) description required. Everything else optional: Unknown fields ignored, skill authored another agent drops unchanged. allowed-tools exception: warned honoured, every mutation already funnels staging explicit commit. description always-model-facing text, keep model needs order decide whether read skill. One description truncated blockr.assistant_skill_description_max_chars (default 1024); always-cost together, assistant also logs warning global catalogue passes blockr.assistant_skill_catalogue_max_chars (default 4000). Scoping skill blocks extensions bears takes budget entirely. surface. skill scoped neither blocks extensions global: appears ## Skills section default system prompt. block-scoped skill appears describe_block_type() describe_block(), next block’s guidance taking precedence ; extension-scoped one appears describe_extension(). Either way model loads body read_skill tool decides skill relevant, user-invocable skill can also run name chat input – /adam-exposure summarise treatment arm. come . package ships one skill (layout, view panel grammar). Point assistant directory option matching environment variable: deployment skill shadows built-name. directory exist error mount rather silently inert configuration. Edits picked without restarting app. Skills instructions, capabilities. skill carries system-prompt weight: writing one editing assistant’s behaviour, adding documentation. widen tool surface, invariants matter enforced code – nothing applies board without commit, whatever skill says. Authorship operator-construction: directory comes server-side option, never board state, tool writes skill.","code":"skills/   adam-exposure/     SKILL.md     column-conventions.md --- name: adam-exposure description: >-   How exposure tables are built here. Read before configuring a   block over ADEX. blocks:   - dplyr_filter_block requires:   - blockr.dplyr (>= 0.2.0) ---  Always filter to `SAFFL == \"Y\"` before summarising; see `column-conventions.md` for the column glossary. blockr.core::blockr_option(\"assistant_skills\", \"/srv/blockr/skills\")"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"extend-per-block-class","dir":"Articles","previous_headings":"","what":"Extend per block class","title":"Get started with blockr.assistant","text":"package exports two S3 generics let block / stack authors customise detailed descriptions model can request demand: default methods report class, name, arguments, external control incoming links; override class carries extra state worth surfacing. compact one-line-per-entity board summary dynamic system prompt produced internally, blockr.core’s str_value() generic – value-returning compact counterpart str(), owned package – assistant-specific generic. influence block, stack layout renders , define str_value() method home package: correct extension point dependency direction (assistant sits core / dock / dag, generic packages implement). dock_stack’s colour reaches summary exactly way. worked example overriding detailed description bespoke block class:","code":"describe_block.fancy_block <- function(x, board, id, ...) {   c(     NextMethod(),     \"\",     sprintf(\"Rule semantics: %s\", attr(x, \"rule_doc\"))   ) }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/articles/intro.html","id":"where-to-go-next","dir":"Articles","previous_headings":"","what":"Where to go next","title":"Get started with blockr.assistant","text":"Roadmap design pillars initial release. per-phase design notes Design notes contributors understanding architectural decisions. Issue tracker feature requests bug reports.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/authors.html","id":null,"dir":"","previous_headings":"","what":"Authors","title":"Authors and Citation","text":"Nicolas Bennett. Author, maintainer. Bristol Myers Squibb. Funder.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/authors.html","id":"citation","dir":"","previous_headings":"","what":"Citation","title":"Authors and Citation","text":"Bennett N (2026). blockr.assistant: LLM Assistant Extension 'blockr'. R package version 0.1.0, https://bristolmyerssquibb.github.io/blockr.assistant/.","code":"@Manual{,   title = {blockr.assistant: An LLM Assistant Extension for 'blockr'},   author = {Nicolas Bennett},   year = {2026},   note = {R package version 0.1.0},   url = {https://bristolmyerssquibb.github.io/blockr.assistant/}, }"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/index.html","id":"blockrassistant","dir":"","previous_headings":"","what":"An LLM Assistant Extension for blockr","title":"An LLM Assistant Extension for blockr","text":"ellmer-powered chat panel blockr.dock boards. assistant ships dock_extension slots board layout like panel; chat UI built shinychat.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/index.html","id":"installation","dir":"","previous_headings":"","what":"Installation","title":"An LLM Assistant Extension for blockr","text":"can install development version blockr.assistant GitHub :","code":"# install.packages(\"pak\") pak::pak(\"BristolMyersSquibb/blockr.assistant\")"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/index.html","id":"example","dir":"","previous_headings":"","what":"Example","title":"An LLM Assistant Extension for blockr","text":"Mount assistant small board. chat wired whichever ellmer model board’s llm_model option resolves (default ellmer::chat_openai()); set blockr.chat_function option swap providers. Assistant panel mounted next small blockr.dock board.","code":"# A small pipeline wired up before the assistant mounts, so it has # something to navigate to (not just from). Try: # # - \"What is on the board?\" -- the answer should come from the prompt's #   Board section, no tool call required. # - \"What is the unique value of `Species` in the data block?\" -- the #   model should call `query_data(code = \"unique(data$Species)\")`. # - \"Add a scatter plot of Sepal.Length vs Petal.Length grouped by #   Species\" -- the model stages add_block (and add_link) calls; flush #   happens at turn end. # - \"Rename `head` to `top_rows`\" -- the model declines the in-place #   rename and offers remove + add, triggered by the id-immutability #   paragraph in the default prompt's intro (no tool call needed).  library(blockr.core) library(blockr.dock) library(blockr.assistant)  board <- new_dock_board(   blocks = c(     data = new_dataset_block(\"iris\"),     filt = new_subset_block(subset = \"Sepal.Length > 5\"),     head = new_head_block(n = 10L),     plot = new_scatter_block(x = \"Sepal.Length\", y = \"Sepal.Width\")   ),   links = c(     new_link(\"data\", \"filt\", \"data\"),     new_link(\"filt\", \"head\", \"data\"),     new_link(\"filt\", \"plot\", \"data\")   ),   stacks = c(     prep    = new_stack(c(\"data\", \"filt\"), name = \"Prep\"),     display = new_stack(c(\"head\", \"plot\"), name = \"Display\")   ),   extensions = list(assistant = new_assistant_extension()),   layout = list(     list(\"data\", \"filt\", \"head\", \"plot\"),     \"assistant\"   ) )  serve(board)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/index.html","id":"status","dir":"","previous_headings":"","what":"Status","title":"An LLM Assistant Extension for blockr","text":"assistant feature-complete initial roadmap: read tools (list_blocks, describe_block, query_data, …), mutation tools (add_block, modify_block, …) flushed atomically per turn, system prompt refreshed every materialized board change model always sees current shape board. See roadmap staged plan per-phase design notes shipped phase.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/blockr.assistant-package.html","id":null,"dir":"Reference","previous_headings":"","what":"blockr.assistant: An LLM Assistant Extension for 'blockr' — blockr.assistant-package","title":"blockr.assistant: An LLM Assistant Extension for 'blockr' — blockr.assistant-package","text":"Building docking layout manager provided 'blockr.dock', extension hosts 'ellmer'-powered chat interface alongside 'blockr' board. chat equipped tools allow large language model inspect manipulate board state – adding, removing modifying blocks, links stacks – via update channel used interactive user actions.","code":""},{"path":[]},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/blockr.assistant-package.html","id":"author","dir":"Reference","previous_headings":"","what":"Author","title":"blockr.assistant: An LLM Assistant Extension for 'blockr' — blockr.assistant-package","text":"Maintainer: Nicolas Bennett nicolas@cynkra.com Authors: Nicolas Bennett nicolas@cynkra.com contributors: Bristol Myers Squibb [funder]","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/default_system_prompt.html","id":null,"dir":"Reference","previous_headings":"","what":"Default assistant system prompt — default_system_prompt","title":"Default assistant system prompt — default_system_prompt","text":"Builds four-section system prompt assistant ships default: intro / conventions block, auto-generated tool catalogue client$get_tools(), one-line-per-skill catalogue deployment's globally-scoped skills, compact board summary.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/default_system_prompt.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Default assistant system prompt — default_system_prompt","text":"","code":"default_system_prompt(   board = NULL,   client = NULL,   view_data = NULL,   skills = NULL,   ... )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/default_system_prompt.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Default assistant system prompt — default_system_prompt","text":"board Reactive containing live board, supplied extension server. NULL omits board section. client ellmer::Chat. NULL omits tool catalogue. view_data Reactive holding blockr.dock's live -views layout (list(views, grids)), supplied extension server, NULL. Read board section's view summary, falling back committed board NULL (every view reported layout). skills available skills, supplied extension server. NULL omits skill catalogue. globally-scoped skills listed ; block- extension-scoped ones surface tools describe target. ... Forward-compatibility slot future inputs.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/default_system_prompt.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Default assistant system prompt — default_system_prompt","text":"character scalar.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/default_system_prompt.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Default assistant system prompt — default_system_prompt","text":"argument optional; corresponding section omitted input NULL, default_system_prompt() REPL returns just intro block – useful inspecting default looks like without mounting board. default value new_assistant_extension's system_prompt argument. Custom functions passed place receive (board, client, ...); ... forward-compatibility headroom future phases adding inputs (accept ... custom functions call site can grow without breaking ).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_block.html","id":null,"dir":"Reference","previous_headings":"","what":"Describe a block for the LLM — describe_block","title":"Describe a block for the LLM — describe_block","text":"Generic backing describe_block assistant tool. default method describe_block.block reports block's class, name, arguments, external-control declaration, incoming links. bulk description comes base::format() block; incoming links computed board snapshot supplied alongside. Block authors override class default enough.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_block.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Describe a block for the LLM — describe_block","text":"","code":"describe_block(x, board, id, ...)  # S3 method for class 'block' describe_block(x, board, id, ...)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_block.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Describe a block for the LLM — describe_block","text":"x block. board current board snapshot, resolving link metadata. id id block lives board (name attribute enclosing blocks object – block objects carry id ids must unique block-level fields user-supplied need ). ... future use.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_block.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Describe a block for the LLM — describe_block","text":"Character vector lines (consistent summarise_result() describe_stack()). tool consumes collapses paste(..., collapse = \"\\n\") returning ellmer.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_board.html","id":null,"dir":"Reference","previous_headings":"","what":"Describe a board for the LLM — describe_board","title":"Describe a board for the LLM — describe_board","text":"Generic backing system prompt's board summary. default method describe_board.board reports board's blocks, links, stacks options; describe_board.dock_board adds view extension summaries via NextMethod(). Board sub-classes extend summary supplying method.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_board.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Describe a board for the LLM — describe_board","text":"","code":"describe_board(b, markers, ...)  # S3 method for class 'board' describe_board(b, markers, ...)  # S3 method for class 'dock_board' describe_board(b, markers, ..., view_data = NULL)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_board.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Describe a board for the LLM — describe_board","text":"b board. markers Named character vector per-block markers carrying block's eval status captured conditions, [stale] [1 error], produced block_markers(). ... Passed methods. view_data Reactive holding blockr.dock's live -views layout, NULL read views committed board. Views dock_board concept, consumed dock_board method – appears signature – base board method.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_board.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Describe a board for the LLM — describe_board","text":"Character vector lines, collapsed paste(..., collapse = \"\\n\") summarise_board().","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_result.html","id":null,"dir":"Reference","previous_headings":"","what":"Describe a block result for the LLM — describe_result","title":"Describe a block result for the LLM — describe_result","text":"Generic backing result summaries assistant feeds model: get_block_result tool post-apply review sends . default method delegates btw::btw_this(). package contributing unusual result type can add method describe directly, blockr terms, instead supplying btw::btw_this() method.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_result.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Describe a block result for the LLM — describe_result","text":"","code":"describe_result(x, ...)  # Default S3 method describe_result(x, ...)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_result.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Describe a block result for the LLM — describe_result","text":"x block result (R object). ... Passed methods (e.g. btw::btw_this()).","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_result.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Describe a block result for the LLM — describe_result","text":"Character vector lines, consistent describe_block() describe_stack(); caller collapses paste(collapse = \"\\n\").","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_result.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Describe a block result for the LLM — describe_result","text":"Methods need bound output guard errors: internal summarise_result() wrapper caps text reaches prompt turns failed description surfaced error message. tool review actually call.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_stack.html","id":null,"dir":"Reference","previous_headings":"","what":"Describe a stack for the LLM — describe_stack","title":"Describe a stack for the LLM — describe_stack","text":"Generic backing description column list_stacks tool. default method describe_stack.stack summarises base stack fields (name comma-separated block ids). Stack classes carry extra attributes (e.g. blockr.dag::dag_stack colour) override class surface attributes.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_stack.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Describe a stack for the LLM — describe_stack","text":"","code":"describe_stack(x, ...)  # S3 method for class 'stack' describe_stack(x, ...)"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_stack.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Describe a stack for the LLM — describe_stack","text":"x stack. ... future use.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/describe_stack.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Describe a stack for the LLM — describe_stack","text":"Character vector lines (consistent summarise_result() describe_block()). tool consumes collapses paste(..., collapse = \"\\n\") returning ellmer.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":null,"dir":"Reference","previous_headings":"","what":"Assistant extension — new_assistant_extension","title":"Assistant extension — new_assistant_extension","text":"Mounts ellmer-powered chat panel blockr.dock board. chat client built board's llm_model option wired read mutation tools; system prompt refreshed every materialized board change model always sees current shape board.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Assistant extension — new_assistant_extension","text":"","code":"new_assistant_extension(   system_prompt = default_system_prompt,   messages = NULL,   ... )"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Assistant extension — new_assistant_extension","text":"system_prompt Either function (called refresh (board, client, ...) build prompt) character scalar (used verbatim, refresh). Defaults exported default_system_prompt function. messages Optional list recorded turns (produced ellmer::contents_record()) seed conversation server start. NULL starts empty conversation. also restored board seeds chat saved . ... Forwarded blockr.dock::new_dock_extension().","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Assistant extension — new_assistant_extension","text":"dock_extension object additionally inheriting assistant_extension.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Assistant extension — new_assistant_extension","text":"system_prompt argument controls prompt model sees: function called every refresh (board, client, ...) must return character scalar. default default_system_prompt() composes four-section prompt (intro / tool catalogue / skill catalogue / board summary); caller can pass function shape. character scalar used verbatim static prompt – refresh, auto-appended catalogue board summary. deal \"give dynamic context, gain full prompt control\". Block- extension-scoped skills unaffected: ride tool return values rather prompt. state shape mirrors constructor: system_prompt (caller passed string) round-trips blockr.dock's ser/des. Function-valued system_prompt omitted state restore falls back constructor default (functions serialise robustly across sessions). conversation saved alongside restored chat board reopened. many recent turns written read save time blockr.chat_save_turns option (BLOCKR_CHAT_SAVE_TURNS environment variable), takes 0 none, positive whole number, Inf , defaults 50. Setting 0 worth considering boards shared, since file otherwise carries whatever typed chat. describes deployment rather board, neither constructor argument part state – restore reads whatever file holds. Turns stored opaque jsonlite::serializeJSON() blob. Recorded turns written state directly survive board's JSON round trip – ellmer::contents_replay() rejects integer version comes back, typed props tokens return lists – since replay happens board-server observer, board took whole session restore rather just chat panel. Payloads written earlier shape dropped deserialisation. raw provider response stripped saving, saved window trimmed whole exchanges never opens closes half tool call. live conversation bounded separately, since saving bounds file: every turn re-sent every request, unbounded session costs goes eventually exceeds provider's context window – limit reached accumulation, every later message , leaving chat dead extension remounted. exchange exceeds threshold, older part conversation replaced summary model writes , recent turns kept verbatim. threshold counts provider billed last exchange rather turns, context window actually spent . restored board checked mount well, reopening long conversation land already limit. threshold board option, chat_compact_tokens, user can retune session – trade recall soon chat starts summarising, right answer moves model, swappable runtime. defaults Inf, leaves compaction : threshold suit one provider's context window wrong another's, nothing API reports window, number picked guess – silently inert small-context model needlessly destructive large one. deployment knows models sets starting point blockr.chat_compact_tokens option BLOCKR_CHAT_COMPACT_TOKENS environment variable, user can pick value session. Contrast chat_save_turns, stays deployment setting governs whether conversations may land shared file – decision hand person whose conversation . much survives compaction companion board option, chat_compact_keep (deployment default blockr.chat_compact_keep, 8): count recent turns left verbatim, everything older becoming summary. offered doubling rungs 256, since turn count needs Inf stops meaning much top – keeping 256 turns verbatim already barely compacting. figure preference rather floor: conversation exceeds threshold fewer turns still compacted, bound inert exactly needed. Compaction rewrites browser transcript match, keeps two honest. shinychat appends turn DOM arrives never reads client back, turns dropped client otherwise stay screen unremembered. replay fills transcript restore provider swap leaves empty; tool traffic carries text replayed.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/reference/new_assistant_extension.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Assistant extension — new_assistant_extension","text":"","code":"ext <- new_assistant_extension() blockr.dock::is_dock_extension(ext) #> [1] TRUE"},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/news/index.html","id":"blockrassistant-development-version","dir":"Changelog","previous_headings":"","what":"blockr.assistant (development version)","title":"blockr.assistant (development version)","text":"Nothing bounded live conversation, written board, long session grew expensive every message eventually exceeded provider’s context window – since limit reached accumulation, every later message , leaving chat dead extension remounted. conversation now compacted: exchange exceeds threshold, older turns replaced summary model writes recent turns kept verbatim. threshold counts provider billed last exchange rather turns, context window spent . restored board checked mount , reopening long conversation longer lands already limit. Fixes #101. compaction threshold board option, chat_compact_tokens, can retuned session rather fixed deploy time: trades recall soon chat starts summarising, right value moves model, board option. defaults Inf, leaving compaction . threshold suited one provider’s context window wrong another’s, provider reports window ellmer (see tidyverse/ellmer#1083), number shipped guess – silently nothing small-context model discarding history needlessly large one. deployment knows models sets starting point new blockr.chat_compact_tokens option (BLOCKR_CHAT_COMPACT_TOKENS), user can choose value session. deliberately unlike blockr.chat_save_turns, stays deployment-governs whether conversations may written shared file. much conversation survives compaction board option , chat_compact_keep (blockr.chat_compact_keep), counting recent turns kept verbatim everything older becomes summary. two controls take different shapes quantities : threshold combobox ladder context sizes also accepts typed value, 64k 1.5M included, since real context windows run thousand tokens million Inf expressible; turn count slider doubling rungs 256, since needs sentinel stops meaning much top – keeping 256 turns verbatim already barely compacting. chat transcript comes back conversation. left empty anything remounted chat panel – reopening saved board, switching provider – shinychat builds transcript appending turns DOM arrive, replay rebuild client reached history argument , . model remembered conversation user longer see. extension now replays client’s turns every mount, also lets compaction drop turns without stranding screen. Block eval status now reaches model. block line Board summary carries marker block holds current result ([dormant], [stale], [waiting], [unset], [failed]), list_blocks gained status column, describe_block names status means. three sites read result – get_block_result, query_data post-commit review – report status place generic failure. way tell never configured block -screen one: dormant block’s result() re-enters gated pipeline raises shiny.silent.error whose message empty string, get_block_result rendered “evaluated successfully:” nothing colon, query_data dropped block unexplained skip list, review printed “(result yet – see conditions )” block conditions see. distinction matters blockr.core’s sixth eval status, stale – dormant block upstream since invalidated – model told healthy block failed go reconfigure , reason columns description upstream longer matches. Reading result still never forces evaluation, -screen block stays -screen. Fixes #107. get_block_conditions tool now says report snapshot. -screen block re-evaluate, captured conditions date last run – empty report rendered “active conditions (errors, warnings, messages)”, affirmative -clear, block simply re-run since model broke . routes state real: upstream change marks block stale, edit -screen block leaves dormant, since staleness tracks upstream results rather block’s expression. dormant stale block’s conditions now carry caveat name two applies, empty report reads unknown. Nothing makes block evaluate; , conditions brought date. turn provider rejects outright – exhausted quota, -long context, dropped connection – now reports error chat instead leaving spinner never resolves composer stays locked. stream fails yields anything, ahead first await shinychat’s streaming coroutine, error escapes promise chain renders mid-stream failures: typed message lands ExtendedTask whose result nothing reads, slash command thrown straight deferred append. now caught. stopgap pending posit-dev/shinychat#304, dropped rejection belongs; lands reports failure copy retired rather kept alongside. Fixes #102. Task-specific instruction now file-based. skill directory holding SKILL.md YAML frontmatter plus markdown body, discovered blockr.assistant_skills (option matching environment variable) alongside package’s inst/skills. skill’s one-line description always ; body loaded demand new read_skill(name, file) tool, deployment can teach assistant conventions without paying every turn – without writing R. skill scoped neither blocks extensions listed new ## Skills section default system prompt; block-scoped one surfaces describe_block_type() describe_block() next block’s guidance takes precedence , extension-scoped one describe_extension(). requires gates skill installed packages, using R’s dependency spelling: unmet requirement makes skill absent every catalogue refused read_skill. user-invocable skill also registers /name slash command runs rest typed line. default_system_prompt() gains skills argument. Fixes #99. layout grammar longer spliced every prompt. ~5 KB inst/prompts/layout.md fragment becomes built-layout skill, read demand invocable /layout; add_view list_views point . Turns never touch view tools – , since blockr.dock auto-places panels newly added blocks – longer pay . board saved assistant turn reopened. recorded turns written board state nested structure, survive board’s JSON round trip: jsonlite reads version back integer typed props tokens lists, ellmer::contents_replay() rejects. replay runs board server observer, failure took whole board rather just chat panel. Boards saved shape now open – blockr_deser() drops legacy payload, mistyped beyond worth repairing. Fixes #97. conversation now saved board opaque jsonlite::serializeJSON() blob, round-trips losslessly, restored chat board reopened. many recent turns written read save time new blockr.chat_save_turns option (BLOCKR_CHAT_SAVE_TURNS environment variable), takes 0 none, positive whole number, Inf , defaults 50. describes deployment rather board, neither constructor argument part board state – worth setting 0 boards shared, since file otherwise carries whatever typed chat. turn’s raw provider response stripped saving, saved window trimmed whole exchanges never opens closes half tool call. read tools now follow lean-listing / per-item-detail split: listing carries fields pick item , bloat-prone detail moves companion drill-tool. list_block_types (renamed list_available_blocks) keeps id, name, package, category, one-line description input slots; guidance, per-argument specs worked examples move new describe_block_type(id). list_stacks drops description column new describe_stack(id), list_extensions drops controllable variables’ current values new describe_extension(id) – keeping document extension’s full text every listing row. block extension descriptions stay listings trimmed blockr.assistant_description_max_chars (default 1000), pointing drill-tool full text. Lean listings inherently bounded, row cap JSON truncation needed. Fixes #85. Staged changes longer auto-applied end turn. model ends turn uncommitted staged changes, now prompted resolve explicitly – commit apply (-band review) new discard tool drop – rather board changed behind model’s back review arriving next turn. Every apply now flows single commit path, staging genuinely means nothing applied commit. prompt bounded; unresolved staged changes dropped reminders. retires turn-end backstop --band review injection. Fixes #91. remove_block() now also cleans links stacks staged block turn, just ones already board. Core’s cleanup augment_board_update() sees committed links stacks, staging block, wiring slotting stack removing rejected (“Expecting links refer known block IDs”, “Unknown block … assigned stack …”) – staging validates writes, removal silent -op. model therefore retract block just wired committed, likely want . Staged link modifications staged stack memberships point removed block cleaned well, tool result names links went block. Fixes #88. system-prompt board summary now bounded character budget, large board longer inflates every request. section (blocks, links, stacks, options, views, extensions) trimmed independently blockr.assistant_board_section_max_chars (default 1500), pointing listing tool – long block list can longer crowd later section extensions – replacing previous --nothing fallback dropped whole summary . Fixes #58. system prompt’s board summary now lists board’s dock extensions – one’s name, externally controllable variables, self-description supplies. model thus sees extension’s guidance drive directly prompt – instance workflow diagram’s advice move block via position handle rather panel tools – instead call list_extensions first. describe_board() new generic backing summary, dock_board method adding view extension sections. Fixes #59. assistant reads live board layout blockr.dock’s view_data reactive rather committed board, list_views system prompt’s view summary reflect UI-driven panel rearrangements immediately. fall back committed board every view reported layout. Fixes #60. resize_panel(view, panel, size) sets panel’s group size (ratio (0, 1)) along splitview axis, staging blockr.dock’s resize panel-op verb. add_panel_to_view() gains matching optional size record panel’s target size added. Fixes #69. assistant now applies staged changes explicit commit tool returns touched blocks’ results tool result, -band. model can stage unit work, commit, read built correct – within one turn – instead review arriving synthetic user message next turn. Fixes #73. focus_panel(view, panel) brings panel already view front tab group focuses , switching view isn’t active one. stages blockr.dock’s select panel-op verb – last one assistant tool emitted – view-edit surface now covers add / remove / move / focus. Use surface specific block extension, e.g. one assistant just added evaluated. Fixes #71. Editing existing view now done atomic panel-op tools – add_panel_to_view(), remove_panel_from_view() move_panel() – map one--one onto blockr.dock’s panel operations, carrying optional near / side placement hints (within / left / right / / ). replace modify_view(), whose whole-layout JSON awkward model edit carried geometry dock’s membership-view validation now rejects. call stages one verb pending update, turn’s edits view compose single atomic update flush. Fixes #64. view-layout tools track blockr.dock’s restructured layout API, view carries panel membership separate dock_grid carries arrangement – dock’s bare dock_layout() constructor layout_from_json() gone. add_view still seeds new view’s arrangement layout pass, list_views / validate_layout speak compact JSON spec , now parsed dock_grid. Changing panels existing view holds membership edit, live arrangement staying dock’s (settled-echo grid mirror sole grid writer). Restores clean install R CMD check blockr.dock main. Fixes #65. list_available_blocks now surfaces block construction metadata blockr.core formalised BMS/blockr.core#121. gains guidance column (model-facing construction notes) examples column (complete worked configurations keyed argument name), arguments column now maps argument description machine-readable JSON-Schema type descriptor (arg_string(), arg_enum(), …) rather bare description string – argument’s allowed values shape reach model, just prose. read core’s new block_metadata() / block_arg_*() accessors. Previously examples / prompt attributes registry carried dropped model ever saw , assistant neither worked example construction guidance configuring block; add_block now points model . deprecated registry_metadata() calls replaced block_metadata() block_meta_arguments(). Fixes #54. post-apply review now reports touched block together immediate neighbours – blocks feeding blocks feeds – just block . block wired column element input – usual cause empty errored panel – previously left model guess fix error alone; now also sees input’s result (consumers, confirm change propagated), can correct reference loosen -strict filter next turn instead leaving user blank panel. neighbours folded existing touched-block set, summarised, capped, fed back path – result interpreted model’s behalf. number blocks reported capped (default 50) option blockr.assistant_review_max_blocks. Fixes #51. Result summaries shown model now produced describe_result() S3 generic, alongside existing describe_block() describe_stack(). package contributing unusual result type can add method describe directly, blockr terms; default method delegates btw::btw_this(). output hard-capped reaches prompt, description errors – block may return R object, multi-element character vector previously took whole review – now surfaces error message instead. review stays bounded block cap times per-result budget, one odd result can longer break . character cap applied describe_block list_stacks tool responses, single tool reply can flood prompt either. per-response character budget (default 2000) set option blockr.assistant_summary_max_chars. options also read matching BLOCKR_* environment variable. post-apply review assistant sends now includes short result summary block turn touched – added modified blocks, plus destination added removed link – model can catch block built silently wrong (e.g. filter yields rows), one errors. touched set read core’s normalised board update, reusing link-removal expansion preprocess_board_update() already performs rather recomputing . summary capped ten blocks pointer get_block_result / query_data rest, review invites model inspect downstream results change likely propagate. per-turn auto-correction bound raised two three. Fixes #43. add_block now rejects constructor arguments outside block’s documented set (names list_available_blocks reports), turning silent argument-swallow constructor ... error model can act . list_available_blocks gains inputs column naming block’s input slots, links wired real slot names rather invented ones. JSON arguments arrays objects (filter’s conditions, summarize’s summaries) now kept lists records rather collapsed data frame block state consume. set_board_option now passes board blockr.core’s set_board_option_value(), adopting required-board signature introduced BMS/blockr.core#229 write honours board’s lock policy rather bypassing . Fixes #52. assistant applies staged changes turn end, now reports outcome back model can correct problem introduced without waiting user notice red block. Two failure channels surfaced: board update model triggered rejected failed apply (board$last_update, BMS/blockr.core#200), blocks begin raising errors warnings change re-evaluates. latter come board-level board$conditions reactive added BMS/blockr.core#218: assistant snapshots start user’s turn , post-apply re-evaluation settles, reports conditions appeared since (set-difference keyed core’s condition id). something found injects short follow-turn, bounded per user turn stubborn problem loop. get_block_conditions read tool per-block health markers board summary now read board$conditions source, replacing package’s condition flattener. Fixes #29. Adds LLM tool surface board options: list_board_options reports option’s id, category, current value default, set_board_option sets value, coercing option’s transform. Unlike block / link / stack mutations, option values session-scoped rather part board-update payload, set_board_option writes immediately via blockr.core’s set_board_option_value() instead staging turn-end flush. llm_model option excluded setter, since changing rebuilds assistant’s chat client. board summary gains Options section listing option surface. board summary dynamic system prompt now renders entity blockr.core’s str_value() generic (str_value methods blockr.dock supplies classes), instead assistant-owned summarise_* S3 generics. single method , dispatching classes owned packages one, never extended downstream; now plain internal helpers call str_value(). entity’s compact rendering owned home package – correct extension point dependency direction – stack’s colour now appears automatically. per-block line reports externally-controllable constructor inputs (marked *) rather frozen initial argument values, also removes old blockr.core:::initial_block_state() workaround. describe_block still reports modifiable keys, now via exported external_ctrl_vars() rather raw external_ctrl attribute (#20). Adds LLM tool surface view CRUD layout mutations: six new tools (list_views, add_view, remove_view, modify_view, set_active_view, rename_view) backed extended staging payload grows views = list(add, mod, rm, active) slot alongside existing blocks / links / stacks slots. staged delta flushes atomically via update() channel used interactive actions, composing across slots single lifecycle tick. Layouts move wire JSON spec form owned blockr.dock: tools parse render dock’s exported layout_from_json() / layout_to_json() / layout_panel_ids(), presenting bare block / extension IDs (dock resolves canonical panel IDs flush). default system prompt picks Layout subsection documenting shape Views section board summary (one line per view: name, active marker, panel count). rename_view synthesised add + rm + active carry-avoid extra payload slot; upstream dock receiver free add native rename later. Requires dock views payload slot BMS/blockr.dock#150 plus augment_board_update() / apply_board_update() generics BMS/blockr.core#185 (relaxed validate_board_update_structure() subclass-defined payload slots pass subclass methods). Fixes #18.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/news/index.html","id":"blockrassistant-010","dir":"Changelog","previous_headings":"","what":"blockr.assistant 0.1.0","title":"blockr.assistant 0.1.0","text":"Feature-complete close-initial roadmap (Phases 1-5). biggest changes since Phase 4 cut: system_prompt argument new_assistant_extension() now accepts function (called refresh (board, client, ...)) string (used verbatim static prompt refresh). default new exported function default_system_prompt, composes three-section prompt: intro / conventions block, auto-generated tool catalogue client$get_tools(), compact board summary. prompt refreshed every materialized board change via observer board$board, model always sees current state board – including user’s UI edits turns (follow-requests multi-tool-call turn). Two new exported S3 generics, summarise_block(x, board, id) summarise_stack(x), drive per-entity lines board summary. Block / stack authors override per class customise classes appear prompt context. Mirrors existing describe_block / describe_stack pair (full descriptions surfaced read tools); summarise_* compact projection used live prompt, describe_* drill-used demand. New query_data read tool: evaluates model-supplied R environment built blockr.core::eval_env() every committed block’s result bound id, captures stdout plus auto-printed value last expression, returns captured text (truncated 200 lines). escape hatch questions static board summary can’t carry: unique values, group counts, ad-hoc filters, cross-block joins. Tutorial vignette package root (vignette(\"blockr.assistant\")), surfaced pkgdown Get-Started article. Showcase demo inst/examples/05-polish/. _pkgdown.yml reference index groups surface “Extension” “S3 generics”.","code":""},{"path":"https://bristolmyerssquibb.github.io/blockr.assistant/news/index.html","id":"blockrassistant-0009000","dir":"Changelog","previous_headings":"","what":"blockr.assistant 0.0.0.9000","title":"blockr.assistant 0.0.0.9000","text":"Initial development version (Phases 1-4): extension shell ellmer chat, read-tool layer, staging & dispatch model, mutation tools.","code":""}]
