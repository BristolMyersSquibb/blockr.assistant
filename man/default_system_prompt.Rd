% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system-prompt.R
\name{default_system_prompt}
\alias{default_system_prompt}
\title{Default assistant system prompt}
\usage{
default_system_prompt(
  board = NULL,
  client = NULL,
  view_data = NULL,
  skills = NULL,
  focus = NULL,
  ...
)
}
\arguments{
\item{board}{Reactive containing the live board, as supplied
to the extension server. \code{NULL} omits the board section.}

\item{client}{An \code{ellmer::Chat}. Unused by this composer, and
passed for the benefit of custom ones -- the model and token
state are read off it.}

\item{view_data}{Reactive holding \code{blockr.dock}'s live all-views
layout (a \code{list(views, grids)}), as supplied to the extension
server, or \code{NULL}. Read for the board section's view summary,
falling back to the committed \code{board} when \code{NULL} (before every
view has reported its layout).}

\item{skills}{The available skills, as supplied to the extension
server. \code{NULL} omits the skill catalogue. Only globally-scoped
skills are listed here; block- and extension-scoped ones surface
through the tools that describe their target.}

\item{focus}{Id of the block the user has selected in the board UI,
or \code{NULL} for no selection. Reported to the model as what an
unqualified request refers to; the section is omitted when there
is no selection, or when \code{board} is \code{NULL} or no longer carries
the block.}

\item{...}{Forward-compatibility slot for future inputs.}
}
\value{
A character scalar.
}
\description{
Builds the three-section system prompt the assistant ships by
default: an intro / conventions block, a one-line-per-skill
catalogue of the deployment's globally-scoped skills, and a
compact board summary.
}
\details{
The registered tools are deliberately not catalogued here. They
reach the model as a structured tool manifest alongside the
system prompt, so a prompt-side listing duplicates them -- and
the per-block-type tools come and go within a conversation, so
any such listing would go stale mid-turn.

Each argument is optional; the corresponding section is
omitted when its input is \code{NULL}, so \code{default_system_prompt()}
at the REPL returns just the intro block -- useful for
inspecting what the default looks like without mounting a
board.

This is the default value of \code{new_assistant_extension}'s
\code{system_prompt} argument. Custom functions passed in its
place receive \verb{(board, client, ...)}; the \code{...} is
forward-compatibility headroom for future phases adding
inputs (accept \code{...} in custom functions so the call site can
grow without breaking you).
}
