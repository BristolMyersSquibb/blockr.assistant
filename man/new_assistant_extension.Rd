% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ext-server.R
\name{new_assistant_extension}
\alias{new_assistant_extension}
\title{Assistant extension}
\usage{
new_assistant_extension(
  system_prompt = default_system_prompt,
  messages = NULL,
  save_turns = blockr_option("chat_save_turns", 50L),
  ...
)
}
\arguments{
\item{system_prompt}{Either a function (called each refresh with
\verb{(board, client, ...)} to build the prompt) or a character
scalar (used verbatim, no refresh). Defaults to the
exported \link{default_system_prompt} function.}

\item{messages}{Optional list of recorded turns (as produced by
\code{\link[ellmer:contents_record]{ellmer::contents_record()}}) to seed the conversation with on
server start. \code{NULL} starts with an empty conversation. This is
also how a restored board seeds the chat it was saved with.}

\item{save_turns}{Number of the most recent turns saved with the
board: \code{0} for none, a positive whole number, or \code{Inf} for all.
Defaults to 50, settable deployment-wide through the
\code{blockr.chat_save_turns} option or the \code{BLOCKR_CHAT_SAVE_TURNS}
environment variable. Worth setting to \code{0} where boards are
shared, since the file otherwise carries whatever was typed into
the chat.}

\item{...}{Forwarded to \code{\link[blockr.dock:new_dock_extension]{blockr.dock::new_dock_extension()}}.}
}
\value{
A \code{dock_extension} object additionally inheriting from
\code{assistant_extension}.
}
\description{
Mounts an \code{ellmer}-powered chat panel on a \code{blockr.dock} board.
The chat client is built from the board's \code{llm_model} option and
wired with the read and mutation tools; the system prompt is
refreshed on every materialized board change so the model always
sees the current shape of the board.
}
\details{
The \code{system_prompt} argument controls the prompt the model sees:
\itemize{
\item A \strong{function} is called on every refresh with
\verb{(board, client, ...)} and must return a character scalar. The
default \code{\link[=default_system_prompt]{default_system_prompt()}} composes a three-section
prompt (intro / tool catalogue / board summary); a caller can
pass any function of the same shape.
\item A \strong{character scalar} is used verbatim as a static prompt -- no
refresh, no auto-appended catalogue or board summary. The deal
is "give up dynamic context, gain full prompt control".
}

The \code{state} shape mirrors the constructor: \code{system_prompt} (when
the caller passed a string) round-trips through \code{blockr.dock}'s
ser/des. Function-valued \code{system_prompt} is omitted from \code{state} so
restore falls back to the constructor default (functions don't
serialise robustly across sessions).

The conversation is saved alongside it and restored into the chat
when the board is reopened, bounded by \code{save_turns}. That bound is
deliberately \emph{not} part of \code{state}: it describes the deployment, not
the board, so a board saved where conversations are kept does not
carry that permission into one where they are not.

Turns are stored as an opaque \code{\link[jsonlite:serializeJSON]{jsonlite::serializeJSON()}} blob.
Recorded turns written into \code{state} directly do not survive the
board's JSON round trip -- \code{ellmer::contents_replay()} rejects the
integer \code{version} that comes back, and typed props such as \code{tokens}
return as lists -- and since the replay happens in a board-server
observer, such a board took the whole session down on restore
rather than just the chat panel. Payloads written in that earlier
shape are dropped on deserialisation. The raw provider response is
stripped before saving, and the saved window is trimmed to whole
exchanges so it never opens or closes on half of a tool call.
}
\examples{
ext <- new_assistant_extension()
blockr.dock::is_dock_extension(ext)

}
