% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe-board.R
\name{describe_board}
\alias{describe_board}
\alias{describe_board.board}
\alias{describe_board.dock_board}
\title{Describe a board for the LLM}
\usage{
describe_board(b, markers, ...)

\method{describe_board}{board}(b, markers, ...)

\method{describe_board}{dock_board}(b, markers, ..., view_data = NULL)
}
\arguments{
\item{b}{A \code{board}.}

\item{markers}{Named character vector of per-block condition
markers (e.g. "1 error"), as produced by
\code{block_condition_markers()}.}

\item{...}{Passed to methods.}

\item{view_data}{Reactive holding blockr.dock's live all-views
layout, or \code{NULL} to read views from the committed board. Views
are a \code{dock_board} concept, so this is consumed only by the
\code{dock_board} method -- where it appears in the signature -- not
by the base \code{board} method.}
}
\value{
Character vector of lines, collapsed with
\code{paste(..., collapse = "\\n")} by \code{summarise_board()}.
}
\description{
Generic backing the system prompt's board summary. The default
method \code{describe_board.board} reports the board's blocks, links,
stacks and options; \code{describe_board.dock_board} adds the view and
extension summaries via \code{NextMethod()}. Board sub-classes extend
the summary by supplying their own method.
}
