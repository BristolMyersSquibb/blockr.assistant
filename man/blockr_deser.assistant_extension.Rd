% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serdes.R
\name{blockr_deser.assistant_extension}
\alias{blockr_deser.assistant_extension}
\title{Assistant extension deserialization}
\usage{
\method{blockr_deser}{assistant_extension}(x, data, ...)
}
\arguments{
\item{x}{Object to dispatch on}

\item{data}{List valued data (converted from JSON)}

\item{...}{Forwarded to the \code{dock_extension} method}
}
\value{
A \code{dock_extension} inheriting from \code{assistant_extension}.
}
\description{
Boards saved before the conversation was dropped from extension state
carry a \code{messages} payload of recorded \code{ellmer} turns. Those records do
not survive the JSON round trip -- \code{ellmer::contents_replay()} aborts on
the integer \code{version} \code{jsonlite} reads back, and on the \code{"NA"} strings the
\code{cost} and \code{duration} props become -- and the replay happens in a board
server observer, so the failure takes down the whole board rather than
just the chat panel. Such a board is unopenable as written, and the
payload is unrecoverable in any case, so it is dropped here.
}
