% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe-result.R
\name{llm_block_result}
\alias{llm_block_result}
\alias{llm_block_result.default}
\title{The result the assistant reads for a block}
\usage{
llm_block_result(x, result, server, ...)

\method{llm_block_result}{default}(x, result, server, ...)
}
\arguments{
\item{x}{The block object.}

\item{result}{The block's evaluated result.}

\item{server}{The block's server object, whose \code{state} holds the block's
current settings as reactives.}

\item{...}{For methods.}
}
\value{
The object to describe with \code{\link[=describe_result]{describe_result()}}.
}
\description{
What \code{get_block_result} and the post-commit review describe for a block.
By default that is the block's evaluated result. A block whose evaluated
result is not what it shows can say what the model should read instead:
a display block that passes its input through and draws something else
from it (a composer table, whose result is the data it was fed) would
otherwise leave the model reading its input and unable to check what it
built.
}
